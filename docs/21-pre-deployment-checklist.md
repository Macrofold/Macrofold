# Pre-deployment checklist and release sign-off

Follow the [launch guide](18-launch-guide.md) first, using [integration registration](operations/launch-integrations.md) for the vendor settings. This checklist records what must actually work before customers are invited. **Neon SQL setup is verified through migration 027; complete deployed Vercel/provider acceptance remains open.** See [current implementation status](16-implementation-status.md) and [Neon evidence](operations/neon/verification.md). Configuration files and a successful build do not establish live operation.

## Work through these gates in order

Use `.data/launch/release-record.json` as the private release record. Add a `checks` entry for each gate with release commit, app deployment ID, runtime digest, UTC date, tester, environment, provider request/event IDs, expected/actual outcome and any remaining issue. Store restricted evidence separately and link its location; never include secrets, prompts, customer files or raw authentication callbacks. Existing false/empty entries remain unverified until you record a real pass.

| Order | Action and exact setup reference | Check before continuing |
| --- | --- | --- |
| 1 | [Reconcile the private files](18-launch-guide.md#1-open-the-right-local-files) | Application configuration uses `https://app.macrofold.ai`; the combined `production.env` auth/vault/cron key conflicts are reconciled with the intended Neon OAuth setup. `MIGRATION_DATABASE_URL` is excluded from hosting/runtime imports and available only to administrative commands. Root `.env` is still local. |
| 2 | [Review/push the release](18-launch-guide.md#2-open-your-accounts-and-select-the-release) | Exact tested commit is on [main](https://github.com/Macrofold/Macrofold/tree/main); CI required checks pass; no local-only source change is mistakenly assumed deployed. |
| 3 | [Neon release migrations](operations/neon.md#step-by-step-release-migrations) | Rehearse and apply all release migrations, beyond the recorded 027 if present. Restricted pooled domain and **direct auth** URLs are used; doctor database/OAuth checks pass. |
| 4 | [R2/email](18-launch-guide.md#4-configure-private-object-storage-and-email), [Vercel/domain](18-launch-guide.md#5-create-the-vercel-project-and-deploy-the-control-plane), [image](18-launch-guide.md#6-build-and-register-the-native-runtime-image) | Production-only variables, valid app domain HTTPS, immutable Ready image, private bucket, verified sender, authenticated cron. No migration-owner credential on Vercel. |
| 5 | [Account and API verification](18-launch-guide.md#7-verify-the-control-plane-and-create-your-operator-login) | Actual email verification/reset/MFA, scoped API key and CLI login work. Revoke credentials and confirm rejection. Paid execution/admission remain off outside test windows. |
| 6 | [Register each integration](operations/launch-integrations.md) | Account-specific auth/callbacks/toolkit pins/model IDs/prices configured; test billing isolated from production. Complete the detailed provider gates below. |
| 7 | [Budget and operator reporting](18-launch-guide.md#12-plan-the-controlled-paid-smoke-test), [recovery](18-launch-guide.md#14-verify-backup-and-disaster-recovery) | Explicit test budget; full SQL + R2 + key recovery; pause/drain controls; operator read-only metrics and restricted audiences. |
| 8 | [Packages](18-launch-guide.md#15-publish-customer-packages-and-release-source), [support/legal](18-launch-guide.md#16-account-closure-and-incident-support), [controlled launch](18-launch-guide.md#17-open-the-service-to-real-users) | Customer installation works; support and reviewed policies are reachable; every offered route passed; open only a small measured pilot. |

### Verification classes

- **Offline/local:** documentation/type/build/contract checks and isolated simulator tests. Use [TESTING.md](../TESTING.md) for the exact suite and local dependencies. Do not repoint setup/seed/test commands at the production database. Run [CI and package checks](24-testing-ci.md#enable-github-checks-and-a-live-coverage-badge) on the intended release; a prior different commit's pass is insufficient.
- **Live database, already scoped:** [Neon evidence](operations/neon/verification.md) establishes tested PostgreSQL 18.6 SQL/RLS/OAuth behavior and independent SQL archive restoration. It does not establish the entire PostgreSQL 18 app suite, deployed browser flows, Neon PITR, or paired R2 recovery.
- **Credential-dependent metadata:** verifies a real account/key/permission, but may consume infrastructure requests and does not prove execution.
- **Metered acceptance:** real image/Workflow/Sandbox, storage, inference, email, search, connector actions and analytics can consume allowances or cost money. Obtain an explicit test budget and verify current account limits first. A vendor's advertised free tier is not evidence of remaining free allowance.

Keep unsupported or untested optional integrations unavailable. All integrations were requested for this launch: record a deliberate scope decision if any remains disabled instead of silently marking the full launch complete.

### Configuration handoff still open

Provider keys have been created by the operator but are not yet entered in the app environment. The canonical origin/callback/CORS worksheets agree on `https://app.macrofold.ai`, and the combined worksheet's database URLs match the verified Neon deployment. Before uploading anything, complete [launch step 1](18-launch-guide.md#1-open-the-right-local-files): reconcile its different auth/vault/cron keys, populate the blank administrative owner URL, regenerate the placeholder Vercel export and correct the hosting worksheet's stale import filename. Never import the combined owner-bearing worksheet.

Use [the complete variable/destination inventory](operations/launch-environment.md) to check each value and Vercel scope, including the Corepack build setting. Follow its isolated staging steps before billing acceptance. Save/redeploy after every environment change.

The existing Stripe sandbox's metadata and price IDs are recorded in `.data/launch/stripe-sandbox-settings.json` and `stripe-sandbox.env`; do not duplicate that catalog. Its runtime key, staging URL, webhook and default Portal are still unconfigured. Model catalog and Composio auth-config/version maps are empty. R2 bucket/endpoint credentials, verified email sender, GitHub App identity and runtime image must be supplied independently of API keys. Complete [provider setup](operations/launch-integrations.md) before treating these integrations as available.

### UI, live updates and connector catalog

- [ ] Apply dashboard schema changes and finish [deployed live-refresh acceptance](21-pre-deployment-checklist.md#ui-live-updates-and-connector-catalog): `/account/events` and detailed run streams, proxy flushing, cross-instance delivery, reconnect/cursor replay, permission revocation, background-tab suspension, navigation and cost/capacity. Observe reconnect beyond one Function lifetime; local simulation cannot prove Vercel's streaming behavior.
- [ ] In the actual dashboard check mobile/desktop layouts, keyboard/focus behavior, custom dropdowns, Usage bar chart, project/files/history, and connection statuses/logos.
- [ ] With a real Composio key, exercise all paginated `GET /api/v3.1/toolkits` metadata, search/categories/three-column cards, scrolling to the end and logo fallbacks. Compare the complete catalog with the vendor view. Confirm missing credentials/upstream failure use the bundled snapshot and unconfigured apps explain their setup requirement. See [connector browser](26-connector-browser.md). `pnpm connectors:refresh` refreshes the public snapshot and changes tracked catalog data; review/test/commit it before the release, not after deploying an unrecorded refresh.
- [ ] Run the full isolated release suite with PostgreSQL 18 as a separate acceptance profile before claiming that engine is covered beyond the targeted Neon checks. Keep the working local PostgreSQL 17 fixture unchanged.

## Credentials and deployment checks

- [ ] Finish [Composio project-key setup](operations/launch-integrations.md#composio-app-connections). CLI login and account/project inspection are verified, but CLI 0.4.1 wrote a masked project key rejected with HTTP 401; its key-creation route returned 404. Copy a complete key from Platform Settings → API Keys into the appropriate secret store, then verify authenticated catalog access, callback binding and one explicitly granted read action with a Composio log ID. No provider action has passed yet.

The [Neon database setup](operations/neon.md) is configured and [tested live](operations/neon/verification.md) on PostgreSQL 18.6: migrations 001–027, restricted roles, direct/pooled tenant isolation, direct auth storage, production-origin OAuth credentials and an independent local SQL restore pass. The auth pooler rejects `search_path`, so import the prepared **direct** `AUTH_DATABASE_URL`. The actual deployed Vercel paths, complete PostgreSQL 18 application suite, and paired SQL/R2/key recovery still require acceptance; the local default remains PostgreSQL 17.

Use [launch step 1](18-launch-guide.md#1-open-the-right-local-files) to select the correct private files. Keep `ALLOW_PAID_EXECUTION=false`, `RUN_ADMISSION_ENABLED=false`, and `PUBLIC_SIGNUP_ENABLED=false` during setup. Temporarily enable signup only in an access-controlled acceptance deployment for the registration tests below, then pause it again. Load secrets through the deployment environment or secret manager; do not paste them into shell command history, screenshots or issue reports. Provider reads below require credentials and may also consume hosting/database requests even when the vendor metadata API itself has no operation fee.

Verify the Vercel build uses adapter packaging (`VERCEL=1`) and completes without the missing `next-server.js.nft.json` error or build-time database connections. Then separately verify the deployed `DATABASE_URL`/`AUTH_DATABASE_URL` use the intended restricted hosted database, not `127.0.0.1:55432`; check both Production and Preview scopes as applicable. Lazy authentication initialization permits a build without a database, so packaging success does not establish runtime connectivity or applied migrations. See [build/deployment behavior](22-deployment.md).

| Gate                        | Exact procedure                                                                                                                                                                                                                                                                                                                                   | Pass condition / evidence to retain                                                                                                                                                                                                                                                                                                                                                                      |
| --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| PostgreSQL / Neon           | Apply all numbered migrations, auth migration and OAuth client provisioning with the separate migration login. Run `node .data/launch/run.mjs doctor` with the reconciled deployed runtime settings; test the chosen pooled/direct connection behavior. Attempt a cross-tenant read with two test accounts.                                                     | TLS connection succeeds; application role is non-owner/non-superuser without BYPASSRLS; migrations are current; tenant selectors never reveal the other account; domain transactions work through pooling; auth uses the verified direct connection because its startup `search_path` is rejected by the pooler. Capture a restored independent database test before customer data.                                                                                                                |
| Vercel control plane        | Deploy from the committed lockfile and production environment. Read `/health`, `/openapi.json` and `/reference` over the final canonical HTTPS domain. Complete dashboard login and an API request with a scoped key. Inspect Function/Workflow build output.                                                                                     | Health reports production readiness, no local fixture account is offered, API/reference agree, cookies are secure, production callbacks use the canonical origin, secrets are absent from public bundles/logs. A build pass alone is insufficient.                                                                                                                                                       |
| Auth / CLI / operator OAuth | Register a test customer; verify email; sign out/in; reset password; enroll MFA and exercise recovery. Run device login using the published CLI client, then revoke the grant and try again. Provision a separate operator client and fetch its OAuth discovery/resource metadata.                                                                | Email verification and MFA gates work; expired/reused links fail; CLI streams using its own scopes; a revoked token fails. Customer credentials cannot access `/admin/mcp` or `/admin/v1`; operator REST/MCP token audiences are enforced. Two replicas share rate limiting.                                                                                                                             |
| Resend sender               | Verify sender domain/DNS in the Resend console; a separately authorized admin key can read `GET /domains`, but the application sending-only key is expected to reject that metadata request. Intentionally submit one verification and one reset email through the deployed app to an owned address; inspect links and the failure/retry experience.                                                                                      | Sender status is verified, SPF/DKIM/DMARC are correct for the selected domain, both emails arrive and link to the canonical origin. Sending was **not tested live** here and can consume email usage.                                                                                                                                                                                                    |
| GitHub App and login        | Configure GitHub OAuth callback(s), App installation callback, private key, selected-repository permissions and `/webhooks/github` secret. Install on a dedicated private test repo. Use the dashboard integration flow, `GET /v1/integrations/github/installations` and `/repositories`; inspect the app's repository access.                    | Returning browser identity is bound to the installation; only authorized installations/repos are listed; revoked access fails; private keys/tokens stay out of `.git/config`, archives and logs. This was **not tested with an actual installation**.                                                                                                                                                    |
| Model credentials           | For every configured provider, make the provider's authenticated model-list read using a private API client (`/v1/models` for OpenAI/Anthropic, `/api/v1/models` for OpenRouter). Confirm exact model IDs, endpoint family, supported harness, account access and current rate card in `MODEL_CATALOG_JSON`.                                      | Metadata access succeeds and the chosen IDs/routes are current. This does **not** prove inference. Store BYOK through Connections; the platform's “test connection” intentionally reports that no inference was made. Managed and BYOK acceptance remain separate.                                                                                                                                       |
| Stripe test setup | Reuse the recorded sandbox and prices. Configure its restricted `rk_test_…` key with the [runtime permissions](operations/launch-integrations.md#runtime-key-permissions), default Portal and test destination secret. Point events at `/webhooks/stripe` in the isolated staging deployment. | Price/currency match the catalog; all 14 events use SDK-compatible snapshot payloads; the six runtime API operations work with the chosen key. Test/live databases and credentials are explicitly isolated: no runtime `event.livemode` guard is implemented. Resolve scheduled-downgrade and tax decisions below before enabling billing. |
| Composio configuration      | Configure project key, `COMPOSIO_AUTH_CONFIGS_JSON`, pinned `COMPOSIO_TOOLKIT_VERSIONS_JSON`, and enable callback verification only after validating that project feature. Read the authenticated `/api/v3.1/tools` catalog for every selected toolkit. Complete `/integrations/composio/install` → provider → `/integrations/composio/callback`. | All catalog pages are visible with the pinned schemas. Completion returns a verified `connected_account_id` and toolkit for the exact initiating organization/user/attempt; a different user, reused callback or changed connection fails. Active account alone is not accepted without callback identity verification. Connected-account creation/connector actions may be billable; they were **not performed**. |
| Direct MCP                  | For each supported server, validate its exact public HTTPS URL, auth scheme and actual discovery metadata. In Connections, authorize and fetch `/v1/connections/{id}/tools`, then explicitly select grants. For OAuth, test resource discovery, authorization-code/PKCE callback, refresh and disconnect.                                         | No private/metadata address, redirect-to-private endpoint or issuer/token endpoint substitution is accepted. Later-page tools appear. Grants bind the owning subject, and concurrent updates return one success plus a stale revision instead of losing revocation. Discovery may be billable on that service; no arbitrary live server was tested.                                                      |
| Custom webhooks             | Configure an owned HTTPS receiver with its signing secret, exact subscription and bounded retry behavior. Generate a test event in the configured test profile and verify signature/timestamp/body hashing at the receiver.                                                                                                                       | Retries keep stable delivery identity; duplicates are safe; invalid signatures are rejected; delivery failures are visible without leaking payloads to metrics. Delivery can consume hosted requests; no external receiver was contacted here.                                                                                                                                                           |

Consult the [OpenAPI reference](api/openapi.json) for exact customer/admin payloads and the [CLI guide](14-cli.md) for terminal commands. Discovery and API credentials must stay server-side. Do not place an operator credential or model key in a CLI command that another person will copy.

## Operations that require an explicit test budget

The following gates are **not live-verified**. Provisioning/hosting, storage, model use, search, connector calls and analytics ingestion may incur charges. Set limits in both the platform and each vendor account, agree on a small test budget, and perform these deliberately. A provider's free tier is not a guarantee that the operator's current account has unused allowance.

### Vercel Sandbox, VCR and Workflow

1. Build the native image for Linux AMD64, publish to the intended VCR project, wait for **Ready**, and configure its immutable digest. Verify the SDK workload identity has access to that exact project/team. Image upload/preparation may be billable.
2. Enable paid execution only in the controlled acceptance environment. Submit one minimal project run through `POST /v1/runs` with a small explicit budget and timeout. Verify a previously nonexistent execution name creates exactly one sandbox, with the expected image, two vCPUs and egress policy.
3. Observe Workflow dispatch, lease acquisition, supervisor launch and durable SSE output at `/v1/runs/{id}/stream`. Disconnect/reconnect the stream and verify replay from the event cursor. Test one input-required interaction, cancellation and timeout. Repeat through the CLI and dashboard.
4. Write a test file, finish the run, inspect the checkpoint, then run again and read that exact file plus a native session continuation. Confirm `/v1/runs/{id}/result` and historical events remain readable after execution stops.
5. In the test environment, interrupt one execution and one workflow delivery. Verify no ambiguous prompt or tool side effect is automatically replayed. Partial files must remain either verified in a checkpoint or explicitly recoverable; a failed run must not silently look empty.
6. Verify the original session is fenced: a stopped or replaced session is not resumed by a probe or retried launch. Confirm retained snapshot IDs/expiration, final sandbox stop/delete behavior and no abandoned billable VMs. Run minutely `/internal/maintenance` through the configured scheduler and verify overdue work is reclaimed safely.

The review fixed an SDK mismatch discovered without spending: `@vercel/sandbox` 3.2.1 exposes lookup failure status as `APIError.response.status`. The adapter now recognizes its actual 404 before creating a new sandbox. Regression tests construct the real SDK error, reject 403/429/500 lookup ambiguity, and simulate a lost creation acknowledgement without starting a VM. **This adapter evidence still requires the real-project steps above.** [Vercel Sandbox SDK](https://vercel.com/docs/sandbox/sdk-reference), [Vercel Workflows](https://vercel.com/docs/workflows).

### R2 persistence, staged uploads and recovery

1. Use a separate private test bucket with the production CORS/lifecycle rules. Upload a small file and a file above the direct-upload threshold through the dashboard and CLI. Exercise signed `PutObject`, `GetObject`, `ListObjectsV2` and `DeleteObject` using the actual bucket endpoint through the application.
2. Verify exact-origin browser CORS, signed content length/type, expired URL rejection, hash mismatch rejection and bounded staging completion. Confirm the bucket is private and no bucket credential is present in the sandbox or browser.
3. Read the file after a different run/session and compare SHA-256. Exercise checkpoint restore, Git metadata, native home and portable export/import. Verify ordinary customer files named `manifest.json` do not collide with archive metadata.
4. Inventory objects, test retention in a dedicated disposable organization, and verify referenced checkpoints remain reachable. Do not shorten live customer retention to speed this check. Confirm temporary staging objects expire, while persistent content has no blanket age-deletion policy.
5. Back up SQL, the encrypted object set and recovery keys at a consistent point. Restore into independent SQL/object storage and authenticate, read files, continue a session and inspect historical usage. Record measured recovery time and any missing objects.

R2 supports a subset of S3, so local SigV4 fixtures do not establish deployed bucket policy, CORS, checksums or account permissions. The pinned AWS SDK uses required-only request checksums to preserve R2 compatibility. R2 operations/storage may be billed even for a small probe. [R2 S3 compatibility](https://developers.cloudflare.com/r2/api/s3/api/).

### Model gateway and all native harness routes

For **every enabled harness × provider/model × funding mode**, run this sequence. Do not infer one route's success from another. OpenCode uses the configured OpenAI, Anthropic or OpenRouter protocol, so include each supported mapping that the UI exposes.

1. Submit a minimal run with a text-only response and one allowed local filesystem tool; stream it through the native harness and gateway. OpenAI routes use `POST /v1/responses` or `/v1/chat/completions`; Anthropic uses `/v1/messages` and, when requested by the harness, `/v1/messages/count_tokens`; OpenRouter uses `/api/v1/chat/completions`.
2. Inspect completion, tool events and usage. Compare input/output/cache accounting and the platform credit ledger to the provider's request/usage records. Include a long enough continuation to test cached input where the selected model supports it. Visible reasoning summaries are stored only when a provider exposes them; private internal reasoning is not guaranteed or fabricated.
3. Repeat as customer BYOK, then revoke the BYOK key and repeat a minimal request. It must fail without using the operator key. Confirm BYOK inference is absent from managed model charges while authorized compute/tool costs remain visible.
4. Check an insufficient budget, unsupported endpoint, disabled model and revoked connection. They must fail before funded inference. Confirm platform-hosted server tools, media generation and premium execution features that lack a reviewed rate-aware path are rejected.
5. Test a streaming interruption/unknown-usage case in the controlled environment. The ledger must expose uncertainty and follow reconciliation rules, rather than silently booking zero or resending the prompt. Resolve any circuit breaker only after investigating the actual provider evidence.

The local native-image tests run actual pinned binaries with loopback model fixtures and no external network. They verify adapter/session/tool shapes; they do not prove current paid model availability or account limits. [OpenAI model documentation](https://developers.openai.com/api/docs/models), [Anthropic Models API](https://platform.claude.com/docs/en/api/models/list), [OpenRouter catalog](https://openrouter.ai/docs/api/api-reference/models/list-all-models-and-their-properties).

### GitHub, connectors, MCP, web search and stdio tools

- **GitHub:** Import a dedicated private repo, create a remote worktree, edit a file, fetch/merge a new upstream commit, push a branch and create a draft PR. Test a deliberate merge conflict, ignored-file collision, branch protection denial, webhook duplicate and revoked installation. Expected: files remain intact, Git sync has its own status, push never force-updates, and a retry does not create duplicate PRs. GitHub API operations may be free, but they require actual ownership/credentials and the hosting/storage path may cost money. No private repo was used in live tests. [GitHub REST metadata](https://docs.github.com/en/rest/meta/meta?apiVersion=2022-11-28).
- **Composio:** After identity verification, invoke one explicitly granted read action in a disposable account. Confirm the pinned toolkit version, mapped arguments/result, usage fact and charge. Revoke the tool grant/account and repeat: reject before a new action. Test a later-page tool and failed/cycling discovery. Catalog collection is bounded to 5,000 tools, 8 MiB, 100 pages and one 15-second discovery deadline; excessive/incomplete catalogs fail explicitly, without silently granting a partial list. [Composio v3.1 tools](https://docs.composio.dev/reference/api-reference/tools/getTools).
- **Remote MCP:** With the actual server, invoke one reviewed read-only tool, then test denied, expired and refreshed authorization. Confirm tool-call IDs, content limits, run-specific grants and cancellation are represented correctly. Preserve a failed action as uncertain when it might have executed; do not retry a side effect blindly. Exercise a multi-page catalog because page size belongs to the server. [MCP pagination specification](https://modelcontextprotocol.io/specification/2025-11-25/server/utilities/pagination).
- **Web search, all offered providers:** For Brave, Exa, Tavily, Parallel AI and Firecrawl, create the matching BYOK connection and execute a bounded query through the granted runtime `web_search` tool. Verify the [exact endpoint/profile](operations/launch-integrations.md#additional-byok-search-providers), title/URL/excerpt, count, quota/error handling, cancellation and absence of blind replay. Repeat Brave with managed funding and verify its ledger reservation/charge; the other four have no managed environment key or rate. Verify BYOK charges in the provider account and explain that they fall outside the platform run budget. The connection Test action does not call the vendor. No live search request has been recorded; each offered provider needs separate acceptance.
- **Exa, Tavily, Parallel AI and Firecrawl:** For each provider being enabled, create a BYOK search connection with a dedicated test key and provider-side spending/quota controls. Grant `web_search`, attach it to an authorized native run, and make one deliberate query only when its credit usage is authorized. Check the [documented fixed request profile](operations/launch-integrations.md#additional-byok-search-providers), title/URL/excerpt output, count bounds, tool history, grant revocation and provider invoice/credit usage. Confirm BYOK calls add no platform search fee and never use a managed key. Validate quota/auth rejection and bounded timeout handling in the deployed transport; do not replay an unknown outcome as a new call. Run the smoke test through each harness being released. No authenticated searches were made during implementation; all provider responses in local tests were fixtures. The dashboard Test action checks stored configuration only. Vendor keys, credits, Vercel outbound compatibility and live runtime MCP execution remain unverified.
- **Stdio MCP:** For each allowlisted package/version, invoke one reviewed filesystem or test-service tool inside the actual Sandbox image. Confirm selected environment secrets, approved args, process isolation, file persistence and call deduplication. Revoke the grant and prove it no longer executes. The local filesystem stdio fixture passed with networking disabled; cloud image policy and third-party service credentials remain unverified.

### Stripe money flow and analytics

1. **Stripe test mode:** Create Checkout through `/v1/billing/checkout` for a top-up and a subscription using Stripe's test payment methods. Verify success, cancel, required authentication and asynchronous events; use `/v1/billing/portal` to change/cancel the subscription. Confirm receipts, currency and redirect URLs. Test webhook signature failure, duplicate and out-of-order delivery.
2. Use Stripe's test lifecycle tools to exercise renewal, failed renewal, expiration, refund and dispute events. Compare the immutable platform ledger and credit reservations with the exact test invoices/payment intents/events. Trigger a reconciliation mismatch and verify new paid work is held until resolved. Test mode does not move real money; it still needs the operator's Stripe account and matching test fixtures. **Authenticated sandbox metadata reads are recorded in the private setup record; application Checkout, Portal, signed delivery and money-flow acceptance remain unrun.** Validate the restricted key's read/write permissions through those real staging flows. Enable live keys/prices/webhook secrets only after test-mode sign-off. [Stripe authentication](https://docs.stripe.com/api/authentication).
3. **Product metrics:** Register two test humans and a service/API client; issue known successful/failed/idempotently replayed requests. Compare request facts, active-user definitions, run totals, tokens and daily series in Usage, operator REST and `/admin/mcp`. A service credential must not inflate human DAU/WAU. Verify account PII requires its separate scope; no prompt, file, raw request body or credential enters general metrics.
4. **PostHog, optional:** Confirm the actual US/EU project and explicit daily event budget. Enable metadata export in the test deployment and send one tiny batch via the configured `POST https://us.i.posthog.com/batch/` or EU equivalent. Confirm event UUID, distinct ID, UTC timestamp and allowed metadata appear once; reject sensitive properties; exercise retry/dedup and a disabled sink. Compare exported totals to authoritative SQL. Project-event allowance/billing is account-specific, so **no live batch was sent**. PostHog is optional; SQL reporting works without it. [PostHog capture API](https://posthog.com/docs/api/capture).

## Release sign-off

Before opening signup, retain evidence that every enabled row passed. Re-run the repository's free type/domain/browser/CLI/native/restore checks after configuration or dependency changes, then perform the bounded real-provider journey on the exact release digest. Record unresolved limitations and disable the corresponding feature instead of advertising it as verified.

Confirm operational ownership of domain/DNS, provider accounts, spending alerts, incident contact, backups, migration recovery, legal/support pages and package publishing. Verify pause controls, queue limits and a small staged rollout. Do not treat this checklist, a successful simulator run, public metadata or HTTP auth rejection as production certification.

## Scheduling and three-tier launch gates

Local SQL/simulator evidence is recorded in [scheduler design and scaling procedure](25-scheduling.md). Before real users:

1. Pass the local history/queue regression checks and rerun `pnpm test:load` on a quiet host (the latest combined run exceeded its existing eight/50-slot timing gates), drain old deployed Workflows with admission paused, apply migrations through 027, and deploy the matching API/SDK/worker release, and verify new deadlines are 24 hours while existing deadlines and running jobs remain unchanged.
2. Reuse the recorded sandbox Pro ($29) and Scale ($199) monthly USD prices and configure their matching included-credit/display settings. Resolve [the scheduled-downgrade limitation](operations/launch-integrations.md#portal-and-lifecycle-tests): the current prices are on separate products, so Stripe's built-in period-end downgrade option cannot provide the promised cross-tier behavior. Confirm the default Portal is configured, quantity stays one, and the chosen catalog/scheduling approach passes acceptance. In **Stripe test mode**, exercise Starter → each plan, Pro ↔ Scale, proration, the approved downgrade timing, renewal, cancellation, failed payment, duplicate/out-of-order events and credit expiry. Confirm $10/$50 recurring inclusion exactly once, no extra proration inclusion, and queued-runtime revalidation. Resolve tax configuration and top-up tax/credit accounting before live billing. Application Checkout/Portal calls remain unrun; the runtime Stripe credential is not configured.
3. Verify Vercel sandbox/model account quotas and deployment-region rates. Start a budgeted cloud cohort at low concurrency; verify real native execution timeouts, persistence after timeout/cancellation, capacity fencing across Function/Workflow instances, native process identity after worker/Workflow failure, and no duplicate provider charges. This uses paid sandbox/model capacity and was deliberately not run.
4. Hold a background request queued in the deployed environment, confirm there is no five-second waiting Workflow loop, then allow capacity and confirm a single launch. Finish a running Workflow and verify the next eligible run starts without waiting for the Cron minute. Exercise a short queue deadline and webhook `run.failed`/SSE expiry, reserved-fund release, and history retention. For the 24-hour case verify durable outbox state/dispatch through a restart and the next day. Inspect actual histories across the 128-advance boundary, counting step retries, sleeps and lifecycle events against the [1,449-event budget](21-pre-deployment-checklist.md#scheduling-and-three-tier-launch-gates). Lose a continuation/start acknowledgement and recover through Cron; verify stale owners cannot release successor leases, the native marker starts once, the execution lease remains unchanged, and reservations reconcile once. Exercise cancellation during handoff, maximum-duration execution and maximum supported restore/checkpoint workloads, recording function time, sandbox lifetime, control requests and total cost. Local budget models do not verify a full-size cloud transfer. Deployed replay, retention and event counts remain unverified.
5. Verify both scheduling priority classes using real CLI streams, per-account lowered caps, all three plan ceilings, interactive arrivals during long background work, and multi-worker fairness. Observe global/account/workspace limits and start-wait p95. Do not advertise immediate or reserved capacity. Complete progressive 8 → 16 → 25 → 50 tests with explicit cost limits before enlarging the launch ceiling.
6. Check the operator dashboard and existing management MCP for active/ceiling, eligible backlog, oldest eligible wait and account-level starts/waits. Confirm only metadata is returned and organization filtering works. Record the measured cloud capacity and provider invoices in the verification record; local worker counts are not live-cloud validation.

## Changelog

The following earlier probe is retained as limited evidence, not a current release pass. Re-run the fixed public probe only if new source/provider changes justify it; it never replaces authenticated acceptance.

### Earlier free public endpoint probe

Run the repeatable probe from the repository root:

```sh
pnpm exec tsx scripts/probe-public-integrations.ts
```

The script has a fixed list of HTTPS GET metadata endpoints. It does not load `.env`, read credentials, initialize vendor SDKs, retry, follow redirects, submit prompts, execute tools or create resources. It exercises the same DNS-pinned, bounded HTTP transport used for remote connections. Nonmatching status/JSON shapes fail the command. All eight checks passed at **2026-09-06 07:27:46 UTC**:

| Provider / exact request                                                                 | Observed                     | Evidence obtained                                       | Still unverified                                                                                          |
| ---------------------------------------------------------------------------------------- | ---------------------------- | ------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| GitHub `GET https://api.github.com/meta`                                                 | 200; expected metadata field | Public API reachability and JSON shape                  | App identity, installation authorization, private repository permissions, OAuth and Git push              |
| OpenRouter `GET https://openrouter.ai/api/v1/models`                                     | 200; `data` array present    | Public catalog endpoint available                       | Account/model access, model output, usage metadata and invoicing                                          |
| OpenAI `GET https://api.openai.com/v1/models`                                            | 401; `error` present         | Endpoint reached; missing credentials rejected          | Valid credentials, allowed model IDs, Responses/Chat streaming, tool usage and metering                   |
| Anthropic `GET https://api.anthropic.com/v1/models` with `anthropic-version: 2023-06-01` | 401; `error` present         | Endpoint reached; missing credentials rejected          | Messages/count-tokens, actual model access, cache/token usage and native Claude compatibility             |
| Stripe `GET https://api.stripe.com/v1/balance`                                           | 401; `error` present         | Endpoint reached; missing credentials rejected          | Test/live account configuration, Checkout, Portal, signatures, subscription and reconciliation lifecycle  |
| Composio `GET https://backend.composio.dev/api/v3.1/tools?limit=1`                       | 401; `error` present         | Current API route reached; missing credentials rejected | Project auth configs, paginated authenticated catalog, callback binding, tool execution and revocation    |
| Resend `GET https://api.resend.com/domains`                                              | 401; `message` present       | Endpoint reached; missing credentials rejected          | Verified sender, actual delivery, reset/verification links and delivery failures                          |
| Vercel `GET https://vercel.com/api/v2/sandboxes?limit=1`                                 | 403; `error` present         | SDK list endpoint reached; missing credentials rejected | Project/team entitlement, image readiness, creation, command execution, networking and recovery snapshots |

These are **public metadata and unauthenticated-denial checks**, not successful authenticated vendor integrations. A 401/403 cannot establish that an API key, subscription, callback, rate card or SDK execution path works. No model/sandbox/search/email/analytics operation was attempted. At the time of that probe, credential-presence inspection found no configured Vercel, R2, model, GitHub App/OAuth, Composio, Stripe, Resend, Brave or PostHog credential in the application environment; values were not printed.

R2 has an account-specific endpoint and billable operation classes, so even object read/write tests were deferred. Workflow needs a deployed project and can bill execution. A direct MCP server is user-selected and may charge for discovery or calls; no arbitrary third-party server was connected. Brave requests consume search usage. PostHog capture can consume an event allowance; no event was sent. OpenCode is a native harness using configured model routes, not a separate free inference integration.
