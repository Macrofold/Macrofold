# Coverage gap audit

[Testing and CI](../testing.md) owns current verification totals. This audit chooses tests from missing behavior, rather than repeating existing assertions to increase a score.

## Evidence added

| Area          | Existing evidence retained                                                                                                                                 | Additional protection                                                                                                                                                                                                                                                          |
| ------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Identity      | Role/scope/project matrix, tenant selectors, session/key expiry, device approval/rotation/revocation, PKCE, MFA and single-use recovery codes, invitations | Real Better Auth verification links and password-reset delivery; invalid/replayed reset tokens; old-password rejection after reset. Email delivery uses a deterministic transport fixture.                                                                                     |
| Delegation    | Key scope/project binding and revocation                                                                                                                   | Expired-but-accepted OAuth authority remains usable until explicit revocation; revoked tokens, disabled clients, expired sessions, removed memberships, viewer demotion, and loss of email verification stop delegated work. Setup proves authority exists before revoking it. |
| Finance       | Balanced journals, high-precision integers, duplicate funding/refunds, out-of-order subscription state, settlement rollback, dispute holds                 | Contenders demonstrably blocked on the PostgreSQL organization lock; protected expiry credit and released remainder; multi-lot allocation and expiry idempotency; financial-event/journal identity; partial/excess debt repayment; negative/zero debit boundaries.             |
| Persistence   | Corrupt bytes/hashes/sizes, missing chunks, unsafe paths, duplicate entries, atomic checkpoint publication, Git conflicts, deletion/undo                   | Malformed transfer pages, entries descending through a recorded symlink, oversized link targets, unsafe paths at the restore boundary, independent chunk integrity, multi-digit pages, and successful retry after an interrupted chunk transfer.                               |
| Execution     | Fencing, ambiguous launch acknowledgements, worker recovery, cancellation/input races, queue expiry, Workflow history budgeting                            | Mutation assertions for all tier caps and lower customer overrides, unknown plans, paid-price mapping, and selected-organization policy loading.                                                                                                                               |
| Dashboard/CLI | Existing full journeys, stream recovery, scope rejection, transfers/conflicts, loading/errors, keyboard/mobile/accessibility checks                        | Coverage from actual application/browser/subprocess execution; isolated acceptance state instead of a shared preview dependency.                                                                                                                                               |
| Python        | Idempotent mutation retry, split/replayed streams, credential-safe errors                                                                                  | Refreshing token suppliers, bounded retry-after, invalid origins, operation recovery, binary path encoding, revoked-stream refusal, and cost CLI validation.                                                                                                                   |

## Remaining gaps

- GitHub social login and authenticated email delivery, OAuth consent registries, live Stripe ordering/reconciliation, model/sandbox limits, object-storage transfer failures and actual cloud Workflow replay require the [pre-deployment checks](../../operations/pre-deployment.md). Local protocol fixtures cannot prove hosted vendor behavior.
- Broader combinations in account authentication, external MCP OAuth, GitHub installation callbacks and large lifecycle orchestration still need focused failure assertions. Passing branch floors is not a reason to stop auditing those paths.
- Browser JavaScript coverage is Chromium-only. Async server components require application/browser acceptance; direct function invocation does not prove routing or hydration.
- Native SDK/binary internals are excluded, and abruptly terminated native workers can lose unflushed counters. Their existing network-disabled execution/restore/input acceptance remains essential.
- Restore tests protect publication of individual files and checkpoint indexes; they do not establish whole-directory transactional replacement. Partial restore must only target an unstarted replacement sandbox, as the implementation requires.
- Worker-kill/long-duration load tests remain independent capacity evidence. Do not interpret coverage totals as a new throughput measurement.

The [mutation reference](mutation.md) records surviving mutations and scope decisions. Add evidence at the smallest boundary that can detect the defect: transaction races in real PostgreSQL, file integrity on disposable files, transport fixtures for providers, and browser/terminal tests for wiring and interaction.
