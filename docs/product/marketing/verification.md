# Marketing verification

This record covers the public concept pages and the separate Swarm particle proof. Their checks do not create users, jobs, database fixtures, or provider requests. Final-homepage and dashboard acceptance have their own records.

## Local checks

The updated `tests/browser/concepts.spec.ts` contains 13 passing Chromium journeys, run against the existing local preview. All ten directions exercise product copy, generated image loading, language tabs and keyboard navigation, focus/Escape motion notes, workflow selection, pause, desktop/mobile layout, reduced motion, JavaScript errors, and an axe WCAG A/AA audit. Shared checks cover gallery navigation, clipboard content, unknown routes, JavaScript-free content, pointer hover, and touch toggle behavior.

The suite writes desktop/mobile screenshots to its configured test output directory. Visual inspection also covers every generated original, the five hero compositions, code and explanatory sections, and the animation-note overlay. Automated checks complement visual review; they do not establish usability, conversion, or live infrastructure performance.

Run only the marketing journeys against an already-running unpaid local app:

```sh
APP_ORIGIN=http://localhost:3210 pnpm exec playwright test tests/browser/concepts.spec.ts --output test-results/marketing --reporter=list
```

No new package, database service, provider key, or paid API is required for these checks. Image concepts were created using the built-in image-generation tool; no repository provider credentials were used.

Strict TypeScript checking (`pnpm exec tsc --noEmit --incremental false`) and `pnpm docs:check` also pass. The documentation check verifies generated public pages and local Markdown targets.

Direct HTTP checks pass for the gallery and its documentation, SDK, pricing, login, and API-reference destinations. The separate canonical `/` route returned HTTP 500 because its session lookup could not connect to local PostgreSQL on port 55432. That database was not started or reconfigured during this design task. Recheck the canonical homepage with its local services running; the independent concept routes render successfully without them.

## Ten curved-surface variations

The collection now contains eight revised surface arrangements and the user's two preserved favorites, Crossed planes and Compressed folds. The eight change projected XYZ geometry rather than only shallow depth; their outlets follow separate surface tangents and their wind adds divergent drift while retaining momentum. The favorite modes run the exact original shaders. [Six rendered comparisons](../../../output/swarm-lookdev/surfaces-v2-favorites-final/favorite-comparison-report.json), covering both favorites at nine, fourteen, and seventeen seconds on Chromium/Metal at 960 × 640 with a centered pointer, match the original PNG bytes and decoded RGBA exactly. All four favorite public image files also retain their original hashes.

The [surface UI check](../../../output/swarm-lookdev/verify-surfaces.mjs) exercises the current renderer on Chromium with the verified Apple M1 Pro Metal backend. All ten produce different nine-second frames while reusing one canvas/context. Switching between revised and favorite programs restores the corresponding exact frames; resizing on a favorite, switching to a revised mode, and resizing back restores the original scale and frame. Selection preserves time and playback state. Keyboard wrapping, deep links, formation stopping at fifteen seconds with 40,000 points and full resolution, loop/pause/scrub, original comparison, and transparency controls pass. Desktop axe reports zero violations. [The current report](../../../output/swarm-lookdev/surface-ui-v2-report.json) records exact source hashes and individual check outcomes.

A targeted run on the same frozen sources completes the mobile and fallback checks: the 390-pixel layout has no horizontal overflow and zero axe violations; reduced motion suppresses pointer-driven rendering and retains an exactly identical framebuffer. The current Folded sheet JavaScript-free poster and selected Saddle WebGL-free poster/download pass. No JavaScript errors were observed. The full run's screenshot equality assertion encountered a one-pixel fractional CSS border difference despite an unchanged framebuffer; the current check compares the rendered pixels directly without a tolerance. The report distinguishes the completed desktop/phase run from the targeted mobile/fallback run. Current assets were present; missing-asset fault injection was not included.

Phase inspection and raster measurements cover Folded sheet and Twisted plane. Folded sheet's inlet and body remain present at every sampled second from nine through fourteen; coarse alpha blocks change by a mean 2.85450 across that interval. Twisted plane's corresponding change is 3.24601. Seventeen-second frames show the surfaces opening into broad dust, faint points remain at nineteen, and the sampled twenty- and twenty-one-second frames are empty. Folded sheet also verifies the empty twenty-two-second reset. Visual inspection covers all eight final arrangements plus representative formation, changing folds, and wind phases. Raster differences establish changing images, not physical interaction or subjective design approval.

Eight revised nine-second frames were captured serially in one Chromium/Metal browser at 2880 × 1920; the two favorite exports were preserved. All twenty current image files decode with real alpha spanning 0–255. The ten PNGs total 22,911,778 bytes; the ten 1440 × 960 WebP posters total 5,026,926 bytes. [Capture evidence](../../../output/swarm-lookdev/surfaces-v2-final/capture-report.json) binds the eight revisions to all five runtime inputs, including the preserved shader module. [Asset verification](../../../output/swarm-lookdev/surfaces-v2-final/asset-verification.json) records the complete image set. Video remains deferred; no video was encoded for this collection.

The current 34,383,248-byte source ZIP contains 51 files. [Package verification](../../../output/swarm-lookdev/surfaces-v2-final/source-package-report.json) records ZIP integrity, every manifest hash, five matching runtime inputs, all twenty current images, thirteen JavaScript syntax checks, and twenty portable local-link checks. The first surface ZIP and both older flow ZIPs retain their original hashes. No movies are included, and a separate portable-package browser smoke run was not performed. The [first surface UI evidence](../../../output/swarm-lookdev/surfaces-v1-evidence/surface-ui-report.json) remains archived; it does not verify the eight revisions.

The approved V3 renderer, material builder, and scene retain their prior SHA-256 values. The collection uses a separate renderer, UI, still directory, and source package. Its WebGL depth and perspective are art-directed additions to the reference chart, not recovered hidden geometry or a physical neighbor simulation.

## Approved V3 particle proof

The preserved `/swarm-proof/index.html` passes the complete [proof check](../../../output/swarm-lookdev/verify-ui.mjs) on the verified Apple M1 Pro Metal backend. It covers desktop and 390-pixel mobile layout, the paused nine-second opening, formation stopping at fifteen seconds with full detail restored, twenty-two-second loop controls, pause/scrub, original comparison, transparency grid, actual mouse parallax, reduced-motion suppression, and current WebGL/JavaScript-free poster fallbacks. Desktop and mobile axe audits report zero violations; no JavaScript errors were observed. The unchanged five-second readiness bound passes. Mobile overflow is measured against the document client width to account for the headed browser's scrollbar. Only the current PNG download is visible; video is deferred. [The report](../../../output/swarm-lookdev/ui-v3-report.json) binds these checks to the renderer and material hashes.

Raster checks cover zero, two, four, six, nine, fourteen, fifteen-and-a-half, seventeen, nineteen, and twenty-one seconds. The opening and twenty-one-second frames are fully transparent. At 32 samples from seven through 14.75 seconds, incoming particles stay visible while the first column remains fully transparent. The minimum body alpha is 82.0% of its median, and the minimum occupied-cell count is 85.9% of its median: the body stays populated as particles recycle. A coarse 32 × 32 block comparison of nine and fourteen seconds measures a mean alpha delta of 7.7514, complementing visual inspection of broad fold movement. Dust remains visible at nineteen seconds. [Phase measurements](../../../output/swarm-lookdev/ui-v3-phase-measurements.json) preserve both module hashes. Growth, folding, wind, and high-resolution still captures were visually inspected.

The [V3 motion diagnostic](../../../output/swarm-lookdev/check-motion-v3.mjs) evaluates the current shader equations and quantized material textures. The material builder calibrates inlet travel to a median 286.5 source pixels per second, 1.5× the prior early inlet speed. Sampled median inlet speeds remain approximately 280–295 through the living phase. Material phase velocity stays positive around 8, 10, and 14 seconds; there is no timed hold. A 41-sample steady-state check keeps total weighted occupancy between 38,092 and 38,292, and 22,587 recycle checks find no visible wraps. Wind releases are staggered from approximately 15.22 to 17.36 seconds, followed by individual drift and fading through twenty seconds. No visible release uses the wrap-velocity correction. [Numeric results](../../../output/swarm-lookdev/motion-v3-kinematics.json) bind both renderer and material hashes. These numerical checks do not establish physical simulation or subjective motion quality.

Current capture completed 2880 × 1920 transparent and black-composited nine-second PNGs on the verified Apple M1 Pro Metal backend. The transparent PNG is 2,509,264 bytes; the 1440 × 960 WebP poster is 727,704 bytes. Both retain real alpha. The planned 1440 × 960, twenty-two-second H.264 clip was interrupted before completion and is deferred by user choice; it is not offered as a current download. [Export verification](../../../output/swarm-lookdev/flow-v3-video-report.json) records source hashes and the interruption; previous movie metadata does not establish a V3 video pass. The live 1.92 MB point buffer remains the transparent motion delivery.

A [backend probe](../../../output/swarm-lookdev/capture-backend-probe.json) found that default headless Chromium used SwiftShader software rendering. Metal reduced isolated readback/conversion to approximately 23–80 ms with the same frozen inputs. A [complete pipeline probe](../../../output/swarm-lookdev/capture-pipeline-probe.json) still measured delays in loopback receive, encoder input, and process scheduling. Subsequent sampling measured active swapping at approximately 76 MiB/s in and 110 MiB/s out. Browser readiness also failed intermittently under that load. After capture/probe processes closed, the complete Metal UI pass succeeded with the original bounds. No completed video is claimed.

The earlier seven-second masters and derivatives are preserved separately. Both decode to 210 frames at 30 fps. Their high-resolution VP9 WebM is 181,815,857 bytes and H.264 MP4 is 32,057,066 bytes; smaller 1440 × 960 copies are 33,528,798 and 2,318,120 bytes respectively. Explicit libvpx-vp9 decoding verified real alpha in that earlier WebM. [Prior video verification](../../../output/swarm-lookdev/video-report.json) owns those results; they do not verify the new twenty-two-second motion and are not offered as current downloads.

The user approved this version's faster throughflow, gradual organization, manifold deformation, and wind breakup before requesting the ten surface variations. This is a reference-guided 2.5D study with art-directed paths and shallow depth, not a recovered complete 3D scene or a physical interaction simulation.

JavaScript syntax, local documentation-link, and generated-public-docs checks pass for this iteration. Concurrent edits outside this study were preserved. It adds static proof assets and local capture tools; no Next.js route or server/client composition changed, so no new application build is required. Earlier application build/typecheck results do not establish this study's visual quality or deployed-device performance.

## Measurement limits and production follow-up

The original concept pages retain static posters. The current [Swarm collection](swarm-motion.md) adds ten art-directed curved-surface deformations to the approved reference-derived particle flow. Its current twenty-two-second sequence provides roughly eight seconds of growth, uninterrupted flow and folding until fifteen seconds, five seconds of staggered wind dispersal, and two empty seconds. Crossed planes and Compressed folds are preserved user favorites; the revised eight require human review before selection and promotion. The earlier five generic procedural surfaces did not meet the reference-fidelity target. Verify loading priority, caching, transferred bytes, frame pacing, alpha-video compatibility, accessibility, and reduced-motion behavior on the chosen hosting deployment and representative devices.

Only Chromium is covered here. The selected Swarm direction's [homepage verification](homepages/verification.md) records its current isolated production build and responsive checks. Safari/Firefox, physical devices, final homepage composition, public package publication, and promoting the canonical homepage remain separate work in the [release TODO](../../maintainers/TODO.md).
