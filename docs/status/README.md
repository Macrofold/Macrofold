# Implementation status

[Delta streaming](../features/api/streaming-implementation.md) is implemented for direct Anthropic/OpenRouter chat and bounded generative calls, with explicit native harness capability admission, final-response accounting and all five SDK helpers. Live local Anthropic delivery, completed-result replay and Langfuse usage/cost readback pass. OpenRouter live acceptance is blocked by provider credit; automated regression and hosted lifetime acceptance remain in [maintainer TODO](../maintainers/TODO.md#delta-streaming-implementation). No automated tests were written or run for this change at the operator’s request.

[Decision context validation](../features/decisions/README.md#request-bounds-and-billing) now leaves actual token-window enforcement to the provider instead of rejecting serialized bytes as tokens. Conservative spending reservations and request-size bounds remain. A synthetic direct OpenRouter Jev probe accepted 60,369 input tokens across two questions while rejecting an oversized single state; [verification](../features/decisions/verification.md#context-window-correction) records the distinction and local regression scope.

Reusable compute now has a shared create/pause/resume/destroy API, run keep-warm policy and Render adapter. Both local modes use Docker: long-running workers have no fixed lifetime cutoff, while temporary sandboxes retain bounded lifetimes. Separate reservations, worktree affinity and per-run checkpoint/credential refresh preserve existing accounting and execution boundaries. Compatible follow-ups now retain the native process and loaded conversation for all six harnesses; per-turn capabilities rotate and suspended writers preserve checkpoint integrity. Offline Docker acceptance verifies two-turn native reuse. See [sandbox acceptance](../features/execution/sandboxes/verification.md); Render deployment and Vercel warm reuse are not yet live-accepted. Local live OpenCode/OpenRouter follow-up confirms warm context recall and automatic idle expiry. Cold continuation currently fails because dot-path checkpoint exclusion omits hidden native history; this remains an unresolved [recovery issue](../maintainers/TODO.md#native-startup-failure-diagnostics). Safe failure events are verified in the API and Langfuse.

[Itemized billing usage](../features/billing/usage.md) adds an organization-scoped API for explicit time intervals, resource/customer filters, charge breakdown and detailed model tokens. It reads existing financial facts with migration 039 indexes. Tracing exports now use background request/step lifecycle hooks so Langfuse network delivery does not delay execution phases. Hosted acceptance remains separate.

[Execution tracing](../features/observability/README.md) now exports native model requests, Jev/other decisions, tools, context retrieval, lifecycle and billing diagnostics through a replaceable Langfuse backend. Live API readback verified managed/BYOK Jev usage and charges plus a simulator customer trace with project/worktree/customer/session attribution on every observation. [Verification](../features/observability/verification.md) separates these results from unverified hosted request/Workflow flushing.

[Explicit-context decisions](../features/decisions/README.md) now have a capability-gated implementation for direct typed inference, immutable context/definitions, bounded evidence reads and sequential tasks. They reuse run observation, budgets and scheduling without native compute. Local acceptance passes 1,078 domain tests and all five SDK suites after adding OpenRouter Jev; the broader feature also passed migration/rotation/paired restore, load and optimized browser/CLI journeys. The [acceptance record](../features/decisions/verification.md) records coverage gaps, a small-fixture measurement and remaining staging/provider gates; this is not a claim of hosted enablement.

The launch application works in the free local simulation profile. Budgeted [live provider acceptance](../engineering/testing/live-integrations.md) verifies selected model gateway routes, encrypted BYOK, Resend test sending, Composio public-tool execution and local GitHub consent/profile reading, and R2 read authentication. Production hosting and remaining native/account journeys still require the [launch guide](../operations/launch-guide.md) and [pre-deployment checks](../operations/pre-deployment.md). No paid sandbox or search-provider calls were made in this acceptance pass.

## Implemented capabilities

Jev choice/score decisions also route through the existing OpenRouter managed or exact BYOK credentials with `typesafe/jev-1.13`. The [local live check](../features/decisions/verification.md#openrouter-jev-acceptance) passed both modes through the SDK/API/executor, with complete usage and request/model evidence. No TypeSafe key is required. Native chat and decisions share OpenRouter price-ceiling conversion; decision rollout remains capability gated and hosted acceptance remains open.

[Run startup](../features/execution/runtime.md#startup-latency-and-measurement) now advances ready phases without artificial sleeps and hydrates bounded batches through one provider upload, preserving restore verification and durable progress. Internal phase timings distinguish active work from waits between attempts. Local recovery and scheduling verification is recorded in the [Workflow guide](../features/execution/workflow-history.md#rollout-and-verification); deployed latency measurement and warm environment reuse remain separate follow-ups.

The [customer MCP](../features/mcp/README.md) exposes customer API operations through scoped API keys or PKCE OAuth, preserving tenant policy, idempotency and run authorization. It shares the REST contract; the operator MCP remains read-only. Focused tests cover initialization, discovery, files, runs, consent and revocation. Production client acceptance is recorded in the [MCP implementation guide](../features/mcp/implementation.md).

Reliability tooling now includes encrypted large-model-request staging, full-image OS/Python/JavaScript advisory scanning, a shared offline [paired recovery procedure](../operations/recovery.md), and [immutable staging release automation](../operations/staging-releases.md). The local recovery rehearsal passes file reading and session continuation. Staging automation remains disabled while staging is paused; hosted large-request/recovery acceptance and production release automation remain explicit follow-ups.

[Files and media](../features/media/README.md) adds optional run attachments across the API and all five SDKs, native image input on reviewed Codex/Claude combinations, bounded PDF/DOCX/text extraction, private media previews, and verified downloadable deliverables. It reuses worktree storage, permissions, model reservations and artifact retention. Audio/video analysis, OCR and a dedicated media-generation service remain deferred. [Multimedia verification](../features/media/verification.md) records local acceptance and remaining hosted checks.

The optional [Customer agents integration path](../features/customer-agents/README.md) adds atomic customer bindings, ownership-checked conversation/run/file methods, generated SDK resources, hosted customer consent and optional React connection controls. It composes existing resources rather than adding a core primitive. Migration 034 is applied to disposable test databases; the running preview and hosted databases are not migrated by this change. Simulator/provider fixtures exercise ownership, idempotency, expiry, stale-permission fencing and uncertain account creation; live multi-account provider acceptance remains separate.

The [customer-agent foundations](../engineering/testing/customer-agents.md) add a complete [personal-agent reference app](../../examples/personal-agent/README.md), optional editable file memory, customer-scoped database/MCP recipes and published human/AI setup guides. Customer identity stays in the integrating application. Operators can persist connector enablement and configure saved-trigger quotas; compatible OpenRouter routes enter the catalog through capability/pricing checks. Templates now hand a saved preset and budget into schedule review. The landing page illustrates lasting customer information across conversations and scheduled work. Migration 033 is applied to disposable fixtures and the local preview; hosted/provider acceptance remains separate.

The additive [connector access model](../features/identity-integrations/connection-access.md) separates approved tools, organization/workspace/preset/pair policy, selection defaults and one-run exceptions. It includes owner-scoped management, typed versioned API/SDK operations, persisted previews, verified session provenance and dispatch-time revocation. [Implementation and acceptance](../engineering/testing/connection-access.md) records migration 032 and verification boundaries. Cloud/provider acceptance remains in maintainer TODO.

Small file edits now prepare immutable content and Git state outside SQL transactions and publish under revision checks with a bounded GC lease. Concrete resource/operation models connect domain results to the generated API types. The dashboard shares typed data access and an editor-independent document controller. [Agent permissions](../features/execution/permissions.md) intersect workspace, worktree and run restrictions; connector and granular file restrictions work across all six harnesses using native tool controls and the shared checked file implementation. File-restricted runs disable shell access. See the [codebase map](../architecture/codebase.md#typed-policy-and-preparation-boundaries) and [verification](../engineering/testing/extensibility.md).

The [dashboard](../features/dashboard/README.md) uses the Macrofold cyan/graphite/ember interface system. Its [collapsible, resizable navigation](../features/dashboard/sidebar.md) retains bottom account/organization switching and Light, Dark, and System appearance. Workspaces default to a list with a remembered grid option. [API-key creation](../features/dashboard/README.md#create-an-api-key) offers Read-only, Read & write, Full access and Custom presets with individual permissions behind a disclosure; the existing server authorization boundary remains authoritative. The file view uses the remaining page height, a resizable folder explorer, breadcrumbs, file/folder creation, file rename, and safe Markdown Rich/Source views. [Worktree implementation](../features/workspaces/implementation.md) owns the authorized mutation and persistence behavior; [dashboard implementation](../features/dashboard/implementation.md) records Orca source provenance.

Home adds an API-backed readiness checklist, a gently animated run-review input, five featured examples including a personal assistant, and copyable coding-agent integration guidance. Shared controls provide muted fading focus indicators, company logos, quick hover transitions, animated navigation icons, readable waiting sheen, hidden scrollbars, and green copy checkmarks that reset after three seconds. Reduced motion preserves color and opacity fades. Templates prefill existing presets without starting work or granting connections. Ask Macrofold remains an explicitly local UI preview with help links; a hosted account assistant remains [future work](../product/improvements.md#dashboard). Existing execution, billing, and authorization boundaries are preserved. The [UI verification record](../features/dashboard/ui-verification.md) owns current unit, browser, CLI/SDK, build evidence and remaining acceptance.

[Hermes, DeepSeek Harness and Pi](../features/execution/harnesses.md) are available through dashboard, CLI, API and all five generated SDKs. All native adapters implement the [Unified Harness Interface](../features/execution/unified-harness-interface.md), which documents the existing contract and community contribution requirements. They reuse the existing model gateway, scheduler and checkpoint system. Network-disabled native tests verify file tools, MCP invocation, portable session continuation, authentication failure and cancellation. [Harness acceptance](../engineering/testing/harnesses.md) owns the current verification scope and remaining deployment checks.

[Named connections](../features/identity-integrations/named-connections.md) preserve stable account selection across renaming, Composio reconnection, presets and CLI invocation. Claude subscription configurations and backup policies can be saved, but native subscription authentication, credential persistence and automatic fallback are **not implemented or enabled**. Native checkpoints now exclude known authentication files. [Evidence and remaining work](../engineering/testing/named-connections.md) distinguish this groundwork from the prior live PoC.

The [built-in model catalog](../features/execution/models.md) supplies reviewed native routes, hourly provider discovery and a shared PostgreSQL cache through existing maintenance. Model JSON and price-version labels no longer require environment variables. Runs freeze accepted prices for execution and reconciliation; OpenRouter requests carry matching provider price ceilings. Migration 029 and [catalog acceptance](../engineering/testing/model-catalog.md) cover cache recovery, contract generation, free metadata probes and remaining deployed checks.

Contributor [agent guidance](../engineering/agent-guidance.md) now routes implementation and review through shared architecture, simplification, TypeScript, web, integration, and durable-state rules. Scoped instructions and Claude imports are checked with documentation links; measured structural verification and remaining harness-loading acceptance are recorded in that guide.

The [feature index](../features/README.md) covers the API, terminal CLI, five language SDKs, dashboard, verified identity/teams, scoped credentials, six native harness adapters, persistent workspaces/checkpoints, optional GitHub/MCP/Composio integrations, managed/BYOK accounting and read-only operator analytics/MCP.

[Web search](../features/identity-integrations/web-search.md) supports Brave Search, Exa, Tavily, Parallel AI and Firecrawl through the shared tool broker and dashboard provider selector. All support encrypted customer keys; managed search remains Brave-only. Provider calls are covered by deterministic fixtures, with authenticated vendor execution and external credit accounting still listed in the pre-deployment checklist.

Starter/Pro/Scale plans allow 2/10/50 concurrent jobs and 30/60/120-minute execution caps, with configurable lower customer limits. New background jobs have a 24-hour queue deadline. PostgreSQL scheduling provides weighted organization turns, interactive priority, workspace ordering, visible waiting reasons and cancellation/fund release. [Scheduling](../features/execution/scheduling.md) owns policy and measured load evidence.

[Workflow waiting/history](../features/execution/workflow-history.md) keeps blocked work in PostgreSQL with adaptive expiry-bounded backoff. Running Workflows hand off the same persisted execution after 128 phase advances, with generation-fenced ownership and a conservative 1,449-event budget including retries/sleeps. Migrations 026–027 add ownership and reassert the 24-hour default without rewriting accepted deadlines, terminal history or reservations.

The [dashboard refresh channel](../features/dashboard/live-refresh.md) shares one SSE subscription across shell navigation, observes committed changes across instances/workers, invalidates queries in bounded batches, and reconciles periodically. It preserves detailed per-run streaming and replay. Migration 025 adds current resource revision stamps and indexes, without an event-log table or another service. Documentation is organized through the [codebase map](../README.md) and mandatory [agent rules](../../.agents/rules/documentation.md).

The [public documentation](../README.md) shares product guides between Cloud and self-hosting, with separate setup paths, a shorter API quickstart, and a [copyable AI integration brief](../getting-started/agents.md). A [shared-workspace guide](../features/workspaces/shared-agents.md) explains sequential handoffs and independent worktrees for parallel agents. One publication manifest supplies search, Markdown exports, and agent/sitemap discovery. All seven documentation browser/API journeys pass against the isolated optimized application, including copied cURL/Python execution, customer-guide search and light/dark mobile accessibility. The shared docs shell now uses the original Saddle lockup, product color tokens, persistent appearance controls and branded code/search surfaces. Eleven documentation unit tests pass. Selected example READMEs are explicitly published by the manifest. [Documentation rules](../../.agents/rules/documentation.md) keep user guides separate from maintainer release work and evidence. [Documentation verification](../engineering/documentation/verification.md) records the isolated acceptance scope.

The [development overview](../getting-started/local-development.md) separates free local simulation, real local Docker execution, and cloud staging, with a guide for each. Local Docker now implements the machine port and uses the SQL phase engine, authorized gateways and encrypted checkpoints. Its explicit overlay preserves free simulation and requires paid opt-in for inference. The six-harness image and network-disabled native fixtures pass on local Linux ARM64. Complete API-driven Docker journeys pass for all six, including worker recovery and fresh-container continuation; [harness acceptance](../engineering/testing/harnesses.md) records exact scope. Live staging remains a separate acceptance gate. [Development-mode evidence](../engineering/testing/development-modes.md) separates domain policy and native-image verification from complete API/cloud acceptance.

The public [marketing concept gallery](../product/marketing/README.md) provides ten developer-infrastructure directions across five hero compositions, informed by focused research across twelve reference sites. Original generated image studies include hover/focus/tap motion descriptions; a shared interactive walkthrough explains execution, persistence, and versioning. Pages use existing Next.js, Radix, and CSS without added dependencies. Concepts remain noindexed and separate from the final homepage. [Marketing verification](../product/marketing/verification.md) records the scoped checks and animation limits.

Swarm is the selected visual direction. Ten [homepage studies](../product/marketing/homepages/README.md) preserve the earlier headline/subtitle, minimal actual SDK examples, and animated workspace/worktree/checkpoint/Git/interface diagrams. Four interaction layouts provide scroll, chapter, tab, and guided versions; narrow screens use readable feature panels. The original gallery remains available. [Homepage verification](../product/marketing/homepages/verification.md) records the 12 new browser journeys, 13 preserved gallery journeys, SDK fixture checks, and isolated build scope.

The preserved [journey design library](../product/marketing/journeys/README.md) adds ten Foundation-based pages at `/journeys`, with three use cases, five illustrated pillars, combined run/stream examples, cyan heading gleams, and hosted registration/docs links. A public searchable connector directory and static metadata export include the complete bundled app catalog and native integrations, without provider credentials or live calls. Earlier libraries remain available. [Journey verification](../product/marketing/journeys/verification.md) records test scope and publication boundaries.

The [final marketing site](../product/marketing/site/README.md) promotes Foundation/Swarm and simple Fanout to the signed-out homepage, with `/site` available while signed in. A configurable [hero playlist](../product/marketing/site/swarm-playlist.md) adds sequential 1.3× Myriad video, responsive sizes, shared pause and a clear loading/failure background; the original image remains archived in the codebase. The canonical marketing hostname `macrofold.ai` serves a verified Vercel build with a static homepage, active R2 custom-domain delivery and confirmed CDN video caching/ranges; `www` redirects to it. The player retries autoplay refusals and temporary delivery failures on normal interaction while preserving explicit Pause. Application registration and execution remain separate release work; the linked guide owns setup and device acceptance. One shared scrolling diagram supports four use cases with typed prompts, drawing file branches, simultaneous filename reveals, parallel worker fan-out, checkpoint/Git convergence, interface inputs, and ten rotating tool logos. Numbered chapter controls and explicit Play/Pause accompany the retained, vertically centered scene. Playback resumes on return; scroll-driven text fades stay independent of Pause. Six benefits cover funding, native triggers, continuation, streaming, scoped access, and sandbox/credential protection. Headings and primary buttons have gently drifting cyan reflections that follow the pointer. Chapter text enters from below and fades toward the top beside the centered scene; pricing shares the dark cyan surface. The supplied Saddle identity and persistent navigation now span the public site. Pricing explains self-serve limits and usage, adds sales-assisted Business/Enterprise offers, and estimates monthly execution and model costs with a separate BYOK provider bill. All three design libraries remain intact. Their code examples now start a new run without a session or saved preset, use public `Macrofold` imports, and name the installed CLI `macrofold`. The current API still requires a workspace and funding choice; provider routing comes from the model catalog. [Final-site verification](../product/marketing/site/verification.md) owns browser, build, and remaining device acceptance evidence.

[Triggers and scheduled tasks](../features/triggers/README.md) add signed Slack events, authenticated incoming webhooks and recurring saved prompts. Migration 028 reuses the SQL dispatch queue and run admission with creator authorization, budgets, worktree serialization, durable receipt deduplication and explicit uncertain-reply recovery. [Trigger acceptance](../engineering/testing/triggers.md) records local and remaining cloud checks.

The current [Swarm particle collection](../product/marketing/swarm-motion.md) at `/swarm-proof/surfaces.html` now preserves Crossed planes and Compressed folds as favorites and revises the other eight with different projected silhouettes and surface arrangements. Forty thousand independently moving points follow shared surface coordinates while broad bends, torsion, and expansion move the folds together. A one-at-a-time switcher preserves playback state and reuses one WebGL context. Every variation retains continuous inflow/outflow, the twenty-two-second sequence, staggered wind breakup, transparency, and adaptive rendering. The eight revisions use broad section-specific exits and more divergent dispersal; the two favorites retain their existing motion. The approved study remains at `/swarm-proof/index.html`. High-resolution stills and a separate source package accompany the new collection; video remains deferred. Verification and remaining visual/device acceptance are recorded with the study.

A separate [manifold iteration](../product/marketing/swarm-manifolds/README.md) at `/swarm-manifolds/index.html` adds ten more defined three-dimensional surfaces with much larger continuous changes in curvature, folds, and orientation. Its Play control resumes from any scrubbed timestamp. It preserves the earlier collection and source packages, and keeps continuous particle flow, the twenty-two-second sequence, transparency, and adaptive rendering. Its own [verification record](../product/marketing/swarm-manifolds/verification.md) owns source-bound captures and control checks; video remains deferred.

The newer [volume collection](../product/marketing/swarm-volumes/README.md) at `/swarm-volumes/index.html` contains five organic bodies and five polygonal solids with entirely new concepts. Closed three-dimensional geometry replaces the sheet-based construction; moving surfaces and an invisible depth pass preserve mass and distinguish foreground faces. It keeps the 40,000-point palette, continuous transport, twenty-two-second sequence, transparency, adaptive rendering, and Play from the selected time. Earlier collections remain unchanged; [volume verification](../product/marketing/swarm-volumes/verification.md) owns the current capture and control evidence.

The separate [Swarm Emergence collection](../product/marketing/swarm-emergence/README.md) at `/swarm-emergence/index.html` adds five organic and five polyhedral assemblies. A dispersed stream gradually develops regional order; sections loosen and rebuild while neighboring surfaces retain definition and the underlying anatomy keeps changing. It preserves the volume collection and all earlier outputs, the 40,000-point treatment, twenty-two-second sequence, transparency, adaptive rendering, and Play from a scrubbed time. [Emergence verification](../product/marketing/swarm-emergence/verification.md) owns its source-bound captures and playback checks; video remains deferred.

The separate [Swarm Resonance collection](../product/marketing/swarm-resonance/README.md) at `/swarm-resonance/index.html` removes invisible depth meshes and depth-dependent particle appearance. It keeps a fixed 40,000-point population with adaptive resolution and broad continuous organization. Two anatomical studies, three natural or alien field studies, and five intricate crystalline assemblies retain the prior playback controls and transparent twenty-two-second sequence. All earlier collections remain unchanged. [Resonance verification](../product/marketing/swarm-resonance/verification.md) owns the capture, rendering-regression, and interface evidence.

The separate [Swarm Metamorphosis collection](../product/marketing/swarm-metamorphosis/README.md) at `/swarm-metamorphosis/index.html` develops the ten Resonance concepts with larger ongoing section transformations, gradual mergers and visibility cycles, round orb sprites, and an ordered lattice inlet. Its twenty-second sequence forms by seven seconds, morphs until fifteen, disperses by nineteen, and leaves one empty second. Earlier collections remain unchanged; [Metamorphosis verification](../product/marketing/swarm-metamorphosis/verification.md) owns the current evidence.

The separate [Swarm Continuum collection](../product/marketing/swarm-continuum/README.md) at `/swarm-continuum/index.html` adds outer sections, increases the fixed population to 60,000 particles, curves the inlet lattice, and broadens its continuous transition into disorder. Alveoli and Rhizome replace Aeolian and Meissner; Hypercell separates three cell families and strengthens their edges. Formation finishes by six seconds, and a 1.0–2.0× slider changes playback rate while preserving position. All earlier collections remain preserved; [Continuum verification](../product/marketing/swarm-continuum/verification.md) owns the current evidence.

The separate [Swarm Confluence collection](../product/marketing/swarm-confluence/README.md) at `/swarm-confluence/index.html` uses 80,000 particles to populate fuller surfaces and gradually settles noise toward peak definition before dispersal. Thalassa and Maelstrom supply calm and turbulent widening currents; Druse replaces Dendrite with eighty-four small layered crystals. The curved grid fades in from darkness, and the studio opens at fourteen seconds with the existing twenty-second timeline and speed controls. All earlier outputs remain preserved; [Confluence verification](../product/marketing/swarm-confluence/verification.md) owns its local evidence.

The separate [Swarm Efflorescence collection](../product/marketing/swarm-efflorescence/README.md) at `/swarm-efflorescence/index.html` adds Plexus to the six-organic/five-polyhedral studio. Its 80,000 particles fade in over twice the prior inlet distance and reach peak definition at eleven seconds, with peak morphing through sixteen and a twenty-one-second loop. Aperiodic grows 96 connected crystals around one shared core, Druse doubles to 168 crystals, and the current studies develop more separated wave surfaces. Earlier outputs remain preserved; [Efflorescence verification](../product/marketing/swarm-efflorescence/verification.md) owns its local evidence.

The separate [Swarm Myriad collection](../product/marketing/swarm-myriad/README.md) at `/swarm-myriad/index.html` retains eleven studies and 80,000 particles, extending the fully defined morphing interval to eleven through twenty seconds in a twenty-five-second loop. Tetrarch spreads its tetrahedral families, Aperiodic extends its connected crystals by fifty percent, and Druse has fifteen clusters. Both current studies form farther left, Maelstrom has stronger waves, and Plexus develops a fine mycelial network of 257 irregular filaments, small offshoots, and local reconnections. Its update preserves the other ten studies. A separate compressed website delivery provides 960- and 1440-pixel H.264 videos at 30 fps, posters, and a visibility-aware player, with all 22 files checked in local Chrome. Earlier outputs remain preserved; [Myriad verification](../product/marketing/swarm-myriad/verification.md) owns its evidence and remaining device checks.

## Verification

The Claude runtime now always supplies its coding-agent prompt and persistent workspace guidance, including runs without custom instructions. A network-disabled regression fails against the previous image and passes with the corrected adapter, covering actual request context, file capture, restore and session continuation. After rebuilding the image, the operator confirmed that the budgeted live Haiku retest created `hello.txt` with the expected contents after refresh; its supplied API result reports final execution success and verified persistence with a checkpoint ID. A subsequent live continuation preserved the original file and appended a second line, confirmed after refresh. Local CLI login, workspace listing and doctor also passed. Dashboard evidence showed released reservations and aggregate charges before continuation; exact per-run settlement, restored conversation recall and hosted acceptance remain separate checks. See [harness acceptance](../engineering/testing/harnesses.md).

The [harness follow-up](../engineering/testing/harnesses.md) passes 695 TypeScript unit/integration/Git tests, four focused browser journeys, all five SDK suites, five CLI subprocess cases, terminal/Python continuation and strict TypeScript. Native tool, MCP, restore, failure and cancellation checks use real binaries with scripted models; current complete API results and unverified cloud boundaries are recorded separately in that guide.

[Persisted file reads](../features/workspaces/read-files.md) use the existing authorized path-based endpoint in all five SDKs. The file-read follow-up fixes Go's empty-download result and passes 49 focused TypeScript tests plus five-language API/simulator journeys; [SDK acceptance](../engineering/testing/sdk-resources.md#file-read-acceptance) records exact scope and remaining hosted checks.

Earlier [trigger acceptance](../engineering/testing/triggers.md) recorded 571 unit/integration/Git tests, five SDK suites and isolated application journeys, with a function-coverage shortfall. The current [connector access acceptance](../engineering/testing/connection-access.md) passes 858 domain tests and all unchanged coverage gates, including the 38% function floor. Live Slack/Vercel acceptance remains a release check.

The [repository review](../engineering/testing/repository-review.md) covers authored source, tests, scripts, SQL and configuration, plus generated/schema validation. Its fixes preserve compute reservations, OAuth binding, file modes, native execution identity and detailed streaming; they also correct save/refetch races, grant recovery, CLI outcomes and stale contract metadata. The latest fixes propagate device-login cancellation/expiry and preserve editor drafts during post-save refresh. Agent rules require an explicit review inventory, isolated discovery credentials and delayed-refresh assertions for query-backed editors.

The earlier [development-mode acceptance](../engineering/testing/development-modes.md) records 516 domain tests, six dashboard/freshness journeys, CLI/terminal/Python checks and its source-specific coverage. That earlier report predates the complete Docker acceptance in [harness verification](../engineering/testing/harnesses.md). Staged native execution remains unverified.

Earlier [SDK/draft acceptance](../engineering/testing/sdk-and-draft-acceptance.md) also passed eight dashboard journeys and 21 installed Python/client/cost tests. Go/Rust/Java passed 7/6/4 tests including actual local API/simulator execution. Those additional suites were not repeated for the development-mode change; preceding full-review measurements remain in [testing and CI](../engineering/testing.md#previous-full-repository-verification).

Existing file edits now autosave after two seconds without typing and show an accessible Saved/error status while preserving newer drafts. Diff generation checks combined checkpoint sizes before reading contents; entries at or above 512,000 bytes return metadata without an inline patch. [SDKs](../features/api/sdks/README.md) are available from source in TypeScript, Python, Go, Rust, and Java under the public `macrofold` package names, with generated resource methods for every public operation, optional origins, environment-key authentication, typed inputs/responses, pinned OpenAPI generation, and independent streaming helpers. [Resource SDK acceptance](../engineering/testing/sdk-resources.md) records the updated five-language local journey and transport/type checks.

All five SDKs provide assistant-text streaming, advanced structured `events`, and `wait` for finalized execution/persistence with typed run failures and local wait timeouts. Saved-preset journeys exercise these helpers through the actual API and simulator; preset responses now expose their persisted revision as the required `version`. The SDK follow-up passes **530 domain tests**, five language journeys, and package/type checks. It adds no backend streaming endpoint and does not establish native Docker/model-gateway or paid cloud acceptance.

Focused [dashboard cookie-authentication acceptance](../engineering/testing/sdk-resources.md#dashboard-session-authentication) passes 27 client tests and three isolated browser journeys, including actual cookie-authenticated tool/event history after refresh, anonymous denial, revoked-stream handling, and queued cancellation. Dashboard session mode preserves normal SDK missing-key validation; existing web bundles need a rebuild to include the source change.

Strict TypeScript, optimized standalone builds, fresh-database installation, independent database/object/key restoration, portable package installation, generated artifacts, documentation and dependency audit pass. Network-disabled native containers now pass all six harnesses, checkpoint restore/continuation, interactive OpenCode input and stdio MCP isolation. The browser fixture owns its build, database, files and loopback port; the running preview is preserved. No paid provider call was made during this repository review.

[Scheduling verification](../features/execution/scheduling.md#verification-and-measured-limits) passes all 11 load/dispatch cases, including eight- and 50-slot cohorts, new-account turns and worker recovery without duplicate starts or observed cap violations. [Workflow history verification](../features/execution/workflow-history.md#rollout-and-verification) covers 24-hour waiting, generation-fenced ownership and the pinned SDK's local history behavior. Local simulation does not establish deployed Workflow quotas, SSE flushing or native sandbox performance.

Earlier [budgeted live acceptance](../engineering/testing/live-integrations.md) verified selected OpenAI/Anthropic/OpenRouter managed and encrypted-BYOK gateway routes, complete usage settlement, Resend test sending, Composio public-tool execution plus local GitHub consent/profile reading, and R2 read authentication. Those records remain distinct from this unpaid local review. Deployed callbacks, native/account journeys, R2 writes and provider quotas still require the pre-deployment checks.

The local preview uses Next.js development mode with PostgreSQL and Mailpit. Isolated application acceptance owns a separate optimized standalone server and simulator worker. Local transcripts, credentials, logs, coverage and screenshots are ignored artifacts. [Deployment validation](../operations/deployment.md#validation-limits) records Vercel packaging evidence; a local build does not establish successful redeployment.

## Remaining launch work

A separate staging Vercel project now has restricted pooled/direct credentials for a new isolated Neon database migrated through 034, a private R2 bucket with exact-origin CORS and temporary-transfer expiry, and a verified email sending domain with a scoped sending key. The bucket-scoped runtime key passed an object write/read/delete check and was denied access to the production bucket. A Linux AMD64 runtime image is Ready under an immutable digest, and the runtime doctor passes. Thirty-six Production-environment settings are saved; existing auth/vault/cron and Stripe secrets were preserved. The staging release is deployed: canonical health reports production readiness, documentation and login return HTTP 200, and an actual minutely Cron invocation returned HTTP 200. A direct maintenance check reports no component failures. The Stripe test destination now uses the canonical origin and is enabled after unsigned-request rejection and a signed synthetic no-customer probe passed; a subsequent application-created $10 sandbox Checkout settled through actual signed delivery, credited the ledger exactly once, and displayed the matching balance. Application Portal entry also passed. The default test Customer Portal is saved and API-verified, with Pro/Scale price switching, payment-method updates and cancellation at period end. The operator account has verified email and enabled MFA. Registration is closed again and a fresh unauthenticated signup receives the expected rejection. Admission and paid execution are enabled for bounded staging acceptance; registration remains closed. Authenticated operator dashboard access and isolated hosted CLI device login, identity/workspace listing and doctor pass. Logout server state confirms refresh revocation and access-token removal; a CLI empty-response reporting fix passes 31 focused tests and a built-subprocess check. See [CLI acceptance](../engineering/testing/sdk-resources.md#hosted-cli-identity-acceptance). A separate Anthropic staging workspace has a $1 monthly cap and an expiring key that passed Haiku metadata discovery. The first deployed native run failed during provisioning without model usage; Vercel rejected IPv6 CIDR entries. The deployed adapter retains IPv4 firewall denies and disables VM IPv6 before credential setup. Budgeted Claude Code / Haiku staging file creation and continuation now pass in separate sandboxes with preserved native conversation identity, verified checkpoints, correct saved contents and confirmed sandbox cleanup. Four model requests reported complete usage; the two successful runs settled $0.198155 in application charges with zero remaining organization/model reservations. A smaller $0.20 test ceiling had been rejected before inference; the documented $0.50 ceiling passed without changing spending enforcement. A 5 MiB staged upload/download matched byte-for-byte, and restoring the earlier verified checkpoint recovered the expected file set while preserving the pre-restore snapshot. See [hosted harness evidence](../engineering/testing/harnesses.md#hosted-claude-acceptance). Password-reset delivery and the remaining subscription/reversal billing cases also remain pending. See [staging setup](../operations/launch-environment.md#staging-and-vercel-project-selection), [billing activation](../operations/launch-integrations.md#activate-and-test-staging-billing) and [release TODO](../maintainers/TODO.md).

The production application is deployed at `https://app.macrofold.ai` in a dedicated Vercel project, separate from marketing and staging. Canonical health, login, documentation, API schema and discovery pages pass; authenticated maintenance reports no component failures. Unauthenticated API/maintenance access is rejected. Registration is now enabled at the operator’s request; the dedicated app homepage redirects signed-out visitors to login, while authenticated visits retain the dashboard. Admission and inference remain disabled pending production model and billing acceptance. Production R2 write/read/delete and denial of staging access pass with a new bucket-scoped key, exact-origin CORS and temporary-transfer expiry. Cloudflare now serves the app DNS and the restored email sending records. The production Linux AMD64 image is Ready under an immutable digest.

The [existing Neon database](../operations/neon.md) is migrated through 034 after isolated branch rehearsal, with restricted pooled/direct connections and production-origin OAuth provisioning. Conflicting worksheet keys were reconciled against successful decryption of the stored verifier; active keys were retained and the owner connection stays outside the serving runtime. See [database verification](../operations/neon/verification.md). The production operator account is registered, with email verification and enabled two-factor authentication confirmed in the authentication database. Authenticated dashboard and Operations access pass. Neon Launch is active with seven-day production history and branch protection. The production Anthropic key is deployed in a separate workspace with an operator-approved $100 monthly limit; Haiku metadata lookup and catalog refresh pass without inference. Stripe live onboarding is complete; live Pro/Scale prices and the default Customer Portal are configured. Runtime key creation, the version-pinned production webhook, capability checks and payment settlement remain pending. Funded production execution, independent recovery and real support/legal destinations remain launch work. Staging is now paused between acceptance sessions: its Vercel deployment returns the expected paused response, cron execution is disabled, and its Neon compute is idle. Production remains healthy with compute fixed at 0.25 CU. The operator approved Launch with a $25/month target; organization spending notifications use a $20 threshold. Existing compute and project-default limits were reset to 0.25 CU after the plan upgrade raised them. Local development retains its existing PostgreSQL 17 profile.

- Secure recovery keys and complete production operator/email/OAuth journeys, approved model rates/BYOK, spending limits, live Stripe prices/Portal, support/legal destinations and any offered integrations before enabling customer execution.
- Build all release artifacts from one reviewed revision, publish the immutable Linux AMD64 runtime image and customer packages, and verify deployed Workflow/cron dispatch.
- Validate dashboard SSE flushing/duration, Neon pooling, deployed cross-instance delivery, revocation and open-tab capacity/costs. Local tests do not establish these cloud properties.
- Rehearse production database/object/key recovery, complete bounded live vendor smoke tests, and invite a small pilot. Provider quota/invoice/CPU readers, enterprise SSO/SCIM, a browser desktop/raw terminal, cross-vendor memory migration and autonomous infrastructure mutations remain outside the launch scope.

Detailed live run history retains its plan policy. The new refresh channel does not change stored output, notifications or accounting retention. macOS/Linux are the initial CLI targets; Windows acceptance and public package provenance remain release steps.
