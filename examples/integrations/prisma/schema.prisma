generator client {
  provider = "prisma-client-js"
}
datasource db {
  provider = "sqlite"
}
model CustomerNote {
  id         String @id @default(uuid())
  customerId String
  title      String
  body       String
  @@index([customerId, id])
}
