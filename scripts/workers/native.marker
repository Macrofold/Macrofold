Verify the current Worker Host runtime with bounded simultaneous Codex and Claude Code workloads, warm reuse, and fresh-container continuation. Use only loopback model fixtures and network-isolated containers; no unit suites, provider credentials, or paid model calls. Repository writes remain exclusively through the connected GitHub connector.
