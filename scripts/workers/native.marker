Verify all six supported native harnesses after fenced unbound preparation cleanup and released-command admission changes. Execute concurrent work, warm reuse, and fresh-container continuation with network-isolated containers and loopback model fixtures; no paid provider calls.
