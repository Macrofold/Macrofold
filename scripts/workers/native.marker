Verify all six supported harnesses after the OpenCode dynamic-port and DeepSeek native-session resolution fixes. Run bounded concurrent workloads, warm reuse, and fresh-container continuation with network-isolated containers and loopback model fixtures. No formal unit suites, paid provider calls, or repository writes from the runner.
