/*
 * Macrofold API
 * Manage persistent workspaces, run cloud agents, stream progress, and integrate tools, billing, and operator reporting. Provider-owned OAuth, internal runtime ingress, and MCP JSON-RPC use separate contracts.
 *
 * The version of the OpenAPI document: 0.9.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package dev.macrofold.model;

import java.net.URLEncoder;
import java.nio.charset.StandardCharsets;
import java.util.StringJoiner;
import java.util.Objects;
import java.util.Map;
import java.util.HashMap;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonValue;
import dev.macrofold.model.WorkerPriceOneOf;
import dev.macrofold.model.WorkerPriceOneOf1;
import java.util.Arrays;
import com.fasterxml.jackson.annotation.JsonPropertyOrder;

import com.fasterxml.jackson.core.type.TypeReference;

import java.io.IOException;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.databind.JsonMappingException;
import com.fasterxml.jackson.databind.MapperFeature;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonToken;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import dev.macrofold.ApiClient;
import dev.macrofold.JSON;

@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.24.0")
@JsonDeserialize(using = WorkerPrice.WorkerPriceDeserializer.class)
@JsonSerialize(using = WorkerPrice.WorkerPriceSerializer.class)
public class WorkerPrice extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(WorkerPrice.class.getName());

    public static class WorkerPriceSerializer extends StdSerializer<WorkerPrice> {
        public WorkerPriceSerializer(Class<WorkerPrice> t) {
            super(t);
        }

        public WorkerPriceSerializer() {
            this(null);
        }

        @Override
        public void serialize(WorkerPrice value, JsonGenerator jgen, SerializerProvider provider) throws IOException, JsonProcessingException {
            jgen.writeObject(value.getActualInstance());
        }
    }

    public static class WorkerPriceDeserializer extends StdDeserializer<WorkerPrice> {
        public WorkerPriceDeserializer() {
            this(WorkerPrice.class);
        }

        public WorkerPriceDeserializer(Class<?> vc) {
            super(vc);
        }

        @Override
        public WorkerPrice deserialize(JsonParser jp, DeserializationContext ctxt) throws IOException, JsonProcessingException {
            JsonNode tree = ctxt.readTree(jp);
            Object deserialized = null;
            boolean typeCoercion = ctxt.isEnabled(MapperFeature.ALLOW_COERCION_OF_SCALARS);
            int match = 0;
            JsonToken token = tree.traverse(jp.getCodec()).nextToken();
            // deserialize WorkerPriceOneOf
            try {
                boolean attemptParsing = true;
                // ensure that we respect type coercion as set on the client ObjectMapper
                if (WorkerPriceOneOf.class.equals(Integer.class) || WorkerPriceOneOf.class.equals(Long.class) || WorkerPriceOneOf.class.equals(Float.class) || WorkerPriceOneOf.class.equals(Double.class) || WorkerPriceOneOf.class.equals(Boolean.class) || WorkerPriceOneOf.class.equals(String.class)) {
                    attemptParsing = typeCoercion;
                    if (!attemptParsing) {
                        attemptParsing |= ((WorkerPriceOneOf.class.equals(Integer.class) || WorkerPriceOneOf.class.equals(Long.class)) && token == JsonToken.VALUE_NUMBER_INT);
                        attemptParsing |= ((WorkerPriceOneOf.class.equals(Float.class) || WorkerPriceOneOf.class.equals(Double.class)) && token == JsonToken.VALUE_NUMBER_FLOAT);
                        attemptParsing |= (WorkerPriceOneOf.class.equals(Boolean.class) && (token == JsonToken.VALUE_FALSE || token == JsonToken.VALUE_TRUE));
                        attemptParsing |= (WorkerPriceOneOf.class.equals(String.class) && token == JsonToken.VALUE_STRING);
                    }
                }
                if (attemptParsing) {
                    deserialized = tree.traverse(jp.getCodec()).readValueAs(WorkerPriceOneOf.class);
                    // TODO: there is no validation against JSON schema constraints
                    // (min, max, enum, pattern...), this does not perform a strict JSON
                    // validation, which means the 'match' count may be higher than it should be.
                    match++;
                    log.log(Level.FINER, "Input data matches schema 'WorkerPriceOneOf'");
                }
            } catch (Exception e) {
                // deserialization failed, continue
                log.log(Level.FINER, "Input data does not match schema 'WorkerPriceOneOf'", e);
            }

            // deserialize WorkerPriceOneOf1
            try {
                boolean attemptParsing = true;
                // ensure that we respect type coercion as set on the client ObjectMapper
                if (WorkerPriceOneOf1.class.equals(Integer.class) || WorkerPriceOneOf1.class.equals(Long.class) || WorkerPriceOneOf1.class.equals(Float.class) || WorkerPriceOneOf1.class.equals(Double.class) || WorkerPriceOneOf1.class.equals(Boolean.class) || WorkerPriceOneOf1.class.equals(String.class)) {
                    attemptParsing = typeCoercion;
                    if (!attemptParsing) {
                        attemptParsing |= ((WorkerPriceOneOf1.class.equals(Integer.class) || WorkerPriceOneOf1.class.equals(Long.class)) && token == JsonToken.VALUE_NUMBER_INT);
                        attemptParsing |= ((WorkerPriceOneOf1.class.equals(Float.class) || WorkerPriceOneOf1.class.equals(Double.class)) && token == JsonToken.VALUE_NUMBER_FLOAT);
                        attemptParsing |= (WorkerPriceOneOf1.class.equals(Boolean.class) && (token == JsonToken.VALUE_FALSE || token == JsonToken.VALUE_TRUE));
                        attemptParsing |= (WorkerPriceOneOf1.class.equals(String.class) && token == JsonToken.VALUE_STRING);
                    }
                }
                if (attemptParsing) {
                    deserialized = tree.traverse(jp.getCodec()).readValueAs(WorkerPriceOneOf1.class);
                    // TODO: there is no validation against JSON schema constraints
                    // (min, max, enum, pattern...), this does not perform a strict JSON
                    // validation, which means the 'match' count may be higher than it should be.
                    match++;
                    log.log(Level.FINER, "Input data matches schema 'WorkerPriceOneOf1'");
                }
            } catch (Exception e) {
                // deserialization failed, continue
                log.log(Level.FINER, "Input data does not match schema 'WorkerPriceOneOf1'", e);
            }

            if (match == 1) {
                WorkerPrice ret = new WorkerPrice();
                ret.setActualInstance(deserialized);
                return ret;
            }
            throw new IOException(String.format(java.util.Locale.ROOT, "Failed deserialization for WorkerPrice: %d classes match result, expected 1", match));
        }

        /**
         * Handle deserialization of the 'null' value.
         */
        @Override
        public WorkerPrice getNullValue(DeserializationContext ctxt) throws JsonMappingException {
            throw new JsonMappingException(ctxt.getParser(), "WorkerPrice cannot be null");
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, Class<?>> schemas = new HashMap<>();

    public WorkerPrice() {
        super("oneOf", Boolean.FALSE);
    }

    public WorkerPrice(WorkerPriceOneOf o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    public WorkerPrice(WorkerPriceOneOf1 o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("WorkerPriceOneOf", WorkerPriceOneOf.class);
        schemas.put("WorkerPriceOneOf1", WorkerPriceOneOf1.class);
        JSON.registerDescendants(WorkerPrice.class, Collections.unmodifiableMap(schemas));
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return WorkerPrice.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * WorkerPriceOneOf, WorkerPriceOneOf1
     *
     * It could be an instance of the 'oneOf' schemas.
     * The oneOf child schemas may themselves be a composed schema (allOf, anyOf, oneOf).
     */
    @Override
    public void setActualInstance(Object instance) {
        if (JSON.isInstanceOf(WorkerPriceOneOf.class, instance, new HashSet<Class<?>>())) {
            super.setActualInstance(instance);
            return;
        }

        if (JSON.isInstanceOf(WorkerPriceOneOf1.class, instance, new HashSet<Class<?>>())) {
            super.setActualInstance(instance);
            return;
        }

        throw new RuntimeException("Invalid instance type. Must be WorkerPriceOneOf, WorkerPriceOneOf1");
    }

    /**
     * Get the actual instance, which can be the following:
     * WorkerPriceOneOf, WorkerPriceOneOf1
     *
     * @return The actual instance (WorkerPriceOneOf, WorkerPriceOneOf1)
     */
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `WorkerPriceOneOf`. If the actual instance is not `WorkerPriceOneOf`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `WorkerPriceOneOf`
     * @throws ClassCastException if the instance is not `WorkerPriceOneOf`
     */
    public WorkerPriceOneOf getWorkerPriceOneOf() throws ClassCastException {
        return (WorkerPriceOneOf)super.getActualInstance();
    }

    /**
     * Get the actual instance of `WorkerPriceOneOf1`. If the actual instance is not `WorkerPriceOneOf1`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `WorkerPriceOneOf1`
     * @throws ClassCastException if the instance is not `WorkerPriceOneOf1`
     */
    public WorkerPriceOneOf1 getWorkerPriceOneOf1() throws ClassCastException {
        return (WorkerPriceOneOf1)super.getActualInstance();
    }



  /**
   * Convert the instance into URL query string.
   *
   * @return URL query string
   */
  public String toUrlQueryString() {
    return toUrlQueryString(null);
  }

  /**
   * Convert the instance into URL query string.
   *
   * @param prefix prefix of the query string
   * @return URL query string
   */
  public String toUrlQueryString(String prefix) {
    String suffix = "";
    String containerSuffix = "";
    String containerPrefix = "";
    if (prefix == null) {
      // style=form, explode=true, e.g. /pet?name=cat&type=manx
      prefix = "";
    } else {
      // deepObject style e.g. /pet?id[name]=cat&id[type]=manx
      prefix = prefix + "[";
      suffix = "]";
      containerSuffix = "]";
      containerPrefix = "[";
    }

    StringJoiner joiner = new StringJoiner("&");

    if (getActualInstance() instanceof WorkerPriceOneOf) {
        if (getActualInstance() != null) {
          joiner.add(((WorkerPriceOneOf)getActualInstance()).toUrlQueryString(prefix + "one_of_0" + suffix));
        }
        return joiner.toString();
    }
    if (getActualInstance() instanceof WorkerPriceOneOf1) {
        if (getActualInstance() != null) {
          joiner.add(((WorkerPriceOneOf1)getActualInstance()).toUrlQueryString(prefix + "one_of_1" + suffix));
        }
        return joiner.toString();
    }
    return null;
  }

}

