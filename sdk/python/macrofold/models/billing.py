# coding: utf-8

"""
    Macrofold API

    Manage persistent projects, run cloud agents, stream progress, and integrate tools, billing, and operator reporting. Provider-owned OAuth, internal runtime ingress, and MCP JSON-RPC use separate contracts.

    The version of the OpenAPI document: 0.9.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from macrofold.models.credit_lot import CreditLot
from macrofold.models.execution_policy import ExecutionPolicy
from macrofold.models.plan import Plan
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class Billing(BaseModel):
    """
    Billing
    """ # noqa: E501
    plan: StrictStr
    available_micro_usd: Annotated[str, Field(strict=True)] = Field(description="Integer micro-USD as a decimal string.")
    reserved_micro_usd: Annotated[str, Field(strict=True)] = Field(description="Integer micro-USD as a decimal string.")
    rate_card_version: StrictStr
    currency: StrictStr
    concurrency_limit: StrictInt
    storage_allowance_bytes: Annotated[str, Field(strict=True)] = Field(description="Non-negative integer count as a decimal string.")
    outstanding_micro_usd: Optional[Annotated[str, Field(strict=True)]] = None
    billing_hold: Optional[StrictBool] = None
    billing_status: Optional[StrictStr] = None
    subscription_price_micro_usd: Optional[Annotated[str, Field(strict=True)]] = None
    credit_lots: Optional[List[CreditLot]] = None
    execution_policy: Optional[ExecutionPolicy] = None
    plans: Optional[List[Plan]] = None
    __properties: ClassVar[List[str]] = ["plan", "available_micro_usd", "reserved_micro_usd", "rate_card_version", "currency", "concurrency_limit", "storage_allowance_bytes", "outstanding_micro_usd", "billing_hold", "billing_status", "subscription_price_micro_usd", "credit_lots", "execution_policy", "plans"]

    @field_validator('plan')
    def plan_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['payg', 'pro', 'scale']):
            raise ValueError("must be one of enum values ('payg', 'pro', 'scale')")
        return value

    @field_validator('available_micro_usd', mode="before")
    def available_micro_usd_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if isinstance(value, str) and not re.match(r"^[0-9]+$", value):
            raise ValueError(r"must validate the regular expression /^[0-9]+$/")
        return value

    @field_validator('reserved_micro_usd', mode="before")
    def reserved_micro_usd_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if isinstance(value, str) and not re.match(r"^[0-9]+$", value):
            raise ValueError(r"must validate the regular expression /^[0-9]+$/")
        return value

    @field_validator('currency')
    def currency_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['USD']):
            raise ValueError("must be one of enum values ('USD')")
        return value

    @field_validator('storage_allowance_bytes', mode="before")
    def storage_allowance_bytes_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if isinstance(value, str) and not re.match(r"^[0-9]+$", value):
            raise ValueError(r"must validate the regular expression /^[0-9]+$/")
        return value

    @field_validator('outstanding_micro_usd', mode="before")
    def outstanding_micro_usd_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if isinstance(value, str) and not re.match(r"^[0-9]+$", value):
            raise ValueError(r"must validate the regular expression /^[0-9]+$/")
        return value

    @field_validator('subscription_price_micro_usd', mode="before")
    def subscription_price_micro_usd_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if isinstance(value, str) and not re.match(r"^[0-9]+$", value):
            raise ValueError(r"must validate the regular expression /^[0-9]+$/")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Billing from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in credit_lots (list)
        _items = []
        if self.credit_lots:
            for _item_credit_lots in self.credit_lots:
                if _item_credit_lots:
                    _items.append(_item_credit_lots.to_dict())
            _dict['credit_lots'] = _items
        # override the default output from pydantic by calling `to_dict()` of execution_policy
        if self.execution_policy:
            _dict['execution_policy'] = self.execution_policy.to_dict()
        # override the default output from pydantic by calling `to_dict()` of each item in plans (list)
        _items = []
        if self.plans:
            for _item_plans in self.plans:
                if _item_plans:
                    _items.append(_item_plans.to_dict())
            _dict['plans'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Billing from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "plan": obj.get("plan"),
            "available_micro_usd": obj.get("available_micro_usd"),
            "reserved_micro_usd": obj.get("reserved_micro_usd"),
            "rate_card_version": obj.get("rate_card_version"),
            "currency": obj.get("currency"),
            "concurrency_limit": obj.get("concurrency_limit"),
            "storage_allowance_bytes": obj.get("storage_allowance_bytes"),
            "outstanding_micro_usd": obj.get("outstanding_micro_usd"),
            "billing_hold": obj.get("billing_hold"),
            "billing_status": obj.get("billing_status"),
            "subscription_price_micro_usd": obj.get("subscription_price_micro_usd"),
            "credit_lots": [CreditLot.from_dict(_item) for _item in obj["credit_lots"]] if obj.get("credit_lots") is not None else None,
            "execution_policy": ExecutionPolicy.from_dict(obj["execution_policy"]) if obj.get("execution_policy") is not None else None,
            "plans": [Plan.from_dict(_item) for _item in obj["plans"]] if obj.get("plans") is not None else None
        })
        return _obj


