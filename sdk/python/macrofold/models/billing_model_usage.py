# coding: utf-8

"""
    Macrofold API

    Manage persistent workspaces, run cloud agents, stream progress, and integrate tools, billing, and operator reporting. Provider-owned OAuth, internal runtime ingress, and MCP JSON-RPC use separate contracts.

    The version of the OpenAPI document: 0.9.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class BillingModelUsage(BaseModel):
    """
    BillingModelUsage
    """ # noqa: E501
    input_tokens: Optional[Annotated[str, Field(strict=True)]] = Field(description="Total input, including cached reads and writes once.")
    output_tokens: Optional[Annotated[str, Field(strict=True)]] = Field(description="Output includes reasoning subsets when reported.")
    cached_input_tokens: Optional[Annotated[str, Field(strict=True)]] = Field(description="Subset of input tokens; null when unavailable.")
    cache_write_input_tokens: Optional[Annotated[str, Field(strict=True)]] = Field(description="Subset of input tokens; null when unavailable.")
    completeness: StrictStr
    provisional: StrictBool
    bound_breached: StrictBool
    budget_cost_micro_usd: Optional[Annotated[str, Field(strict=True)]] = Field(description="Consumes the run budget even for BYOK; not an additional charge.")
    reserved_micro_usd: Optional[Annotated[str, Field(strict=True)]] = Field(description="Original model-call ceiling.")
    reported_micro_usd: Optional[Annotated[str, Field(strict=True)]] = Field(description="Usage at accepted retail rates, before the authorized ceiling.")
    provider_cost_micro_usd: Optional[Annotated[str, Field(strict=True)]] = Field(description="Known provider-rate estimate, never a reconciled invoice or extra Macrofold charge.")
    provider_cost_status: StrictStr
    request_id: StrictStr = Field(description="Durable model-call identity; use to correlate gateway diagnostics and tracing.")
    __properties: ClassVar[List[str]] = ["input_tokens", "output_tokens", "cached_input_tokens", "cache_write_input_tokens", "completeness", "provisional", "bound_breached", "budget_cost_micro_usd", "reserved_micro_usd", "reported_micro_usd", "provider_cost_micro_usd", "provider_cost_status", "request_id"]

    @field_validator('input_tokens', mode="before")
    def input_tokens_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if isinstance(value, str) and not re.match(r"^[0-9]+$", value):
            raise ValueError(r"must validate the regular expression /^[0-9]+$/")
        return value

    @field_validator('output_tokens', mode="before")
    def output_tokens_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if isinstance(value, str) and not re.match(r"^[0-9]+$", value):
            raise ValueError(r"must validate the regular expression /^[0-9]+$/")
        return value

    @field_validator('cached_input_tokens', mode="before")
    def cached_input_tokens_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if isinstance(value, str) and not re.match(r"^[0-9]+$", value):
            raise ValueError(r"must validate the regular expression /^[0-9]+$/")
        return value

    @field_validator('cache_write_input_tokens', mode="before")
    def cache_write_input_tokens_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if isinstance(value, str) and not re.match(r"^[0-9]+$", value):
            raise ValueError(r"must validate the regular expression /^[0-9]+$/")
        return value

    @field_validator('budget_cost_micro_usd', mode="before")
    def budget_cost_micro_usd_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if isinstance(value, str) and not re.match(r"^[0-9]+$", value):
            raise ValueError(r"must validate the regular expression /^[0-9]+$/")
        return value

    @field_validator('reserved_micro_usd', mode="before")
    def reserved_micro_usd_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if isinstance(value, str) and not re.match(r"^[0-9]+$", value):
            raise ValueError(r"must validate the regular expression /^[0-9]+$/")
        return value

    @field_validator('reported_micro_usd', mode="before")
    def reported_micro_usd_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if isinstance(value, str) and not re.match(r"^[0-9]+$", value):
            raise ValueError(r"must validate the regular expression /^[0-9]+$/")
        return value

    @field_validator('provider_cost_micro_usd', mode="before")
    def provider_cost_micro_usd_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if isinstance(value, str) and not re.match(r"^[0-9]+$", value):
            raise ValueError(r"must validate the regular expression /^[0-9]+$/")
        return value

    @field_validator('provider_cost_status')
    def provider_cost_status_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['estimated_from_usage', 'unavailable']):
            raise ValueError("must be one of enum values ('estimated_from_usage', 'unavailable')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of BillingModelUsage from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if input_tokens (nullable) is None
        # and model_fields_set contains the field
        if self.input_tokens is None and "input_tokens" in self.model_fields_set:
            _dict['input_tokens'] = None

        # set to None if output_tokens (nullable) is None
        # and model_fields_set contains the field
        if self.output_tokens is None and "output_tokens" in self.model_fields_set:
            _dict['output_tokens'] = None

        # set to None if cached_input_tokens (nullable) is None
        # and model_fields_set contains the field
        if self.cached_input_tokens is None and "cached_input_tokens" in self.model_fields_set:
            _dict['cached_input_tokens'] = None

        # set to None if cache_write_input_tokens (nullable) is None
        # and model_fields_set contains the field
        if self.cache_write_input_tokens is None and "cache_write_input_tokens" in self.model_fields_set:
            _dict['cache_write_input_tokens'] = None

        # set to None if budget_cost_micro_usd (nullable) is None
        # and model_fields_set contains the field
        if self.budget_cost_micro_usd is None and "budget_cost_micro_usd" in self.model_fields_set:
            _dict['budget_cost_micro_usd'] = None

        # set to None if reserved_micro_usd (nullable) is None
        # and model_fields_set contains the field
        if self.reserved_micro_usd is None and "reserved_micro_usd" in self.model_fields_set:
            _dict['reserved_micro_usd'] = None

        # set to None if reported_micro_usd (nullable) is None
        # and model_fields_set contains the field
        if self.reported_micro_usd is None and "reported_micro_usd" in self.model_fields_set:
            _dict['reported_micro_usd'] = None

        # set to None if provider_cost_micro_usd (nullable) is None
        # and model_fields_set contains the field
        if self.provider_cost_micro_usd is None and "provider_cost_micro_usd" in self.model_fields_set:
            _dict['provider_cost_micro_usd'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of BillingModelUsage from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "input_tokens": obj.get("input_tokens"),
            "output_tokens": obj.get("output_tokens"),
            "cached_input_tokens": obj.get("cached_input_tokens"),
            "cache_write_input_tokens": obj.get("cache_write_input_tokens"),
            "completeness": obj.get("completeness"),
            "provisional": obj.get("provisional"),
            "bound_breached": obj.get("bound_breached"),
            "budget_cost_micro_usd": obj.get("budget_cost_micro_usd"),
            "reserved_micro_usd": obj.get("reserved_micro_usd"),
            "reported_micro_usd": obj.get("reported_micro_usd"),
            "provider_cost_micro_usd": obj.get("provider_cost_micro_usd"),
            "provider_cost_status": obj.get("provider_cost_status"),
            "request_id": obj.get("request_id")
        })
        return _obj


