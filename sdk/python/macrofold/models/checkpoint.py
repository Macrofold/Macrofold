# coding: utf-8

"""
    Macrofold API

    Manage persistent projects, run cloud agents, stream progress, and integrate tools, billing, and operator reporting. Provider-owned OAuth, internal runtime ingress, and MCP JSON-RPC use separate contracts.

    The version of the OpenAPI document: 0.9.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from uuid import UUID
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class Checkpoint(BaseModel):
    """
    Checkpoint
    """ # noqa: E501
    id: UUID
    workspace_id: UUID
    run_id: Optional[UUID] = None
    created_at: datetime
    consistency: StrictStr
    verification: StrictStr
    size_bytes: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="Non-negative integer count as a decimal string.")
    pinned: StrictBool
    git_commit: Optional[StrictStr] = None
    git_status: Optional[StrictStr] = None
    git_error: Optional[StrictStr] = None
    __properties: ClassVar[List[str]] = ["id", "workspace_id", "run_id", "created_at", "consistency", "verification", "size_bytes", "pinned", "git_commit", "git_status", "git_error"]

    @field_validator('consistency')
    def consistency_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['crash_consistent', 'quiescent']):
            raise ValueError("must be one of enum values ('crash_consistent', 'quiescent')")
        return value

    @field_validator('verification')
    def verification_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['pending', 'verified', 'failed']):
            raise ValueError("must be one of enum values ('pending', 'verified', 'failed')")
        return value

    @field_validator('size_bytes', mode="before")
    def size_bytes_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if isinstance(value, str) and not re.match(r"^[0-9]+$", value):
            raise ValueError(r"must validate the regular expression /^[0-9]+$/")
        return value

    @field_validator('git_status')
    def git_status_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['ready', 'attention']):
            raise ValueError("must be one of enum values ('ready', 'attention')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Checkpoint from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if git_error (nullable) is None
        # and model_fields_set contains the field
        if self.git_error is None and "git_error" in self.model_fields_set:
            _dict['git_error'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Checkpoint from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "workspace_id": obj.get("workspace_id"),
            "run_id": obj.get("run_id"),
            "created_at": obj.get("created_at"),
            "consistency": obj.get("consistency"),
            "verification": obj.get("verification"),
            "size_bytes": obj.get("size_bytes"),
            "pinned": obj.get("pinned"),
            "git_commit": obj.get("git_commit"),
            "git_status": obj.get("git_status"),
            "git_error": obj.get("git_error")
        })
        return _obj


