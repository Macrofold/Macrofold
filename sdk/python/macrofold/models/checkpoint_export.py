# coding: utf-8

"""
    Macrofold API

    Manage persistent projects, run cloud agents, stream progress, and integrate tools, billing, and operator reporting. Provider-owned OAuth, internal runtime ingress, and MCP JSON-RPC use separate contracts.

    The version of the OpenAPI document: 0.9.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from uuid import UUID
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class CheckpointExport(BaseModel):
    """
    Tenant-authorized, short-lived download capabilities. Git bundle contains tracked objects/refs only; portable archive includes permitted ignored files and sanitized session state, never infrastructure credentials or archive keys. Expired links require a new idempotent export request.
    """ # noqa: E501
    checkpoint_id: UUID
    format: StrictStr
    download_url: StrictStr
    expires_at: datetime
    size_bytes: Annotated[str, Field(strict=True)]
    sha256: Annotated[str, Field(strict=True)]
    manifest_url: StrictStr
    manifest_sha256: Annotated[str, Field(strict=True)]
    source_commit: Optional[StrictStr] = None
    export_commit: Optional[StrictStr] = Field(default=None, description="Git bundle review commit containing permitted checkpoint edits; may differ from source_commit. Does not mutate hosted branch.")
    __properties: ClassVar[List[str]] = ["checkpoint_id", "format", "download_url", "expires_at", "size_bytes", "sha256", "manifest_url", "manifest_sha256", "source_commit", "export_commit"]

    @field_validator('format')
    def format_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['git_bundle', 'portable_archive']):
            raise ValueError("must be one of enum values ('git_bundle', 'portable_archive')")
        return value

    @field_validator('size_bytes', mode="before")
    def size_bytes_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if isinstance(value, str) and not re.match(r"^[0-9]+$", value):
            raise ValueError(r"must validate the regular expression /^[0-9]+$/")
        return value

    @field_validator('sha256', mode="before")
    def sha256_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if isinstance(value, str) and not re.match(r"^[a-f0-9]{64}$", value):
            raise ValueError(r"must validate the regular expression /^[a-f0-9]{64}$/")
        return value

    @field_validator('manifest_sha256', mode="before")
    def manifest_sha256_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if isinstance(value, str) and not re.match(r"^[a-f0-9]{64}$", value):
            raise ValueError(r"must validate the regular expression /^[a-f0-9]{64}$/")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of CheckpointExport from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of CheckpointExport from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "checkpoint_id": obj.get("checkpoint_id"),
            "format": obj.get("format"),
            "download_url": obj.get("download_url"),
            "expires_at": obj.get("expires_at"),
            "size_bytes": obj.get("size_bytes"),
            "sha256": obj.get("sha256"),
            "manifest_url": obj.get("manifest_url"),
            "manifest_sha256": obj.get("manifest_sha256"),
            "source_commit": obj.get("source_commit"),
            "export_commit": obj.get("export_commit")
        })
        return _obj


