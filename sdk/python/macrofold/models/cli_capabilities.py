# coding: utf-8

"""
    Macrofold API

    Manage persistent projects, run cloud agents, stream progress, and integrate tools, billing, and operator reporting. Provider-owned OAuth, internal runtime ingress, and MCP JSON-RPC use separate contracts.

    The version of the OpenAPI document: 0.9.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class CliCapabilities(BaseModel):
    """
    CliCapabilities
    """ # noqa: E501
    api_version: StrictStr
    minimum_cli_version: StrictStr
    recommended_cli_version: StrictStr
    features: List[StrictStr]
    stream_rotation_seconds: StrictInt
    max_transfer_files: StrictInt
    max_transfer_bytes: StrictInt
    max_file_bytes: StrictInt
    __properties: ClassVar[List[str]] = ["api_version", "minimum_cli_version", "recommended_cli_version", "features", "stream_rotation_seconds", "max_transfer_files", "max_transfer_bytes", "max_file_bytes"]

    @field_validator('stream_rotation_seconds')
    def stream_rotation_seconds_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set([55]):
            raise ValueError("must be one of enum values (55)")
        return value

    @field_validator('max_transfer_files')
    def max_transfer_files_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set([1000]):
            raise ValueError("must be one of enum values (1000)")
        return value

    @field_validator('max_transfer_bytes')
    def max_transfer_bytes_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set([262144000]):
            raise ValueError("must be one of enum values (262144000)")
        return value

    @field_validator('max_file_bytes')
    def max_file_bytes_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set([26214400]):
            raise ValueError("must be one of enum values (26214400)")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of CliCapabilities from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of CliCapabilities from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "api_version": obj.get("api_version"),
            "minimum_cli_version": obj.get("minimum_cli_version"),
            "recommended_cli_version": obj.get("recommended_cli_version"),
            "features": obj.get("features"),
            "stream_rotation_seconds": obj.get("stream_rotation_seconds"),
            "max_transfer_files": obj.get("max_transfer_files"),
            "max_transfer_bytes": obj.get("max_transfer_bytes"),
            "max_file_bytes": obj.get("max_file_bytes")
        })
        return _obj


