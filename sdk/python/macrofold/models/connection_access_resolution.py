# coding: utf-8

"""
    Macrofold API

    Manage persistent projects, run cloud agents, stream progress, and integrate tools, billing, and operator reporting. Provider-owned OAuth, internal runtime ingress, and MCP JSON-RPC use separate contracts.

    The version of the OpenAPI document: 0.9.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, StrictBool, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List
from uuid import UUID
from macrofold.models.connection_access_match import ConnectionAccessMatch
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class ConnectionAccessResolution(BaseModel):
    """
    ConnectionAccessResolution
    """ # noqa: E501
    connection_id: UUID
    name: StrictStr
    tools: List[StrictStr]
    source: StrictStr
    ready: StrictBool
    rejection_codes: List[StrictStr]
    access_match: ConnectionAccessMatch
    can_override: StrictBool
    __properties: ClassVar[List[str]] = ["connection_id", "name", "tools", "source", "ready", "rejection_codes", "access_match", "can_override"]

    @field_validator('source')
    def source_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['organization', 'project', 'agent', 'project_agent', 'run_override', 'none']):
            raise ValueError("must be one of enum values ('organization', 'project', 'agent', 'project_agent', 'run_override', 'none')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ConnectionAccessResolution from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of access_match
        if self.access_match:
            _dict['access_match'] = self.access_match.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ConnectionAccessResolution from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "connection_id": obj.get("connection_id"),
            "name": obj.get("name"),
            "tools": obj.get("tools"),
            "source": obj.get("source"),
            "ready": obj.get("ready"),
            "rejection_codes": obj.get("rejection_codes"),
            "access_match": ConnectionAccessMatch.from_dict(obj["access_match"]) if obj.get("access_match") is not None else None,
            "can_override": obj.get("can_override")
        })
        return _obj


