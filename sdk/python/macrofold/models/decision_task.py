# coding: utf-8

"""
    Macrofold API

    Manage persistent workspaces, run cloud agents, stream progress, and integrate tools, billing, and operator reporting. Provider-owned OAuth, internal runtime ingress, and MCP JSON-RPC use separate contracts.

    The version of the OpenAPI document: 0.9.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from uuid import UUID
from macrofold.models.task_outcome_receipt import TaskOutcomeReceipt
from macrofold.models.task_run import TaskRun
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class DecisionTask(BaseModel):
    """
    DecisionTask
    """ # noqa: E501
    id: UUID
    workspace_id: UUID
    objective: Annotated[str, Field(min_length=1, strict=True, max_length=4000)]
    status: StrictStr
    next_step: Annotated[str, Field(min_length=1, strict=True, max_length=200)]
    max_cost_micro_usd: Annotated[str, Field(strict=True)]
    committed_micro_usd: Annotated[str, Field(strict=True)]
    outstanding_micro_usd: Annotated[str, Field(strict=True)]
    max_runs: StrictInt
    evidence_expires_at: datetime
    latest_wake_id: Optional[UUID]
    proposal_artifact_id: Optional[UUID]
    failure_code: Optional[StrictStr]
    runs: List[TaskRun]
    outcomes: List[TaskOutcomeReceipt]
    __properties: ClassVar[List[str]] = ["id", "workspace_id", "objective", "status", "next_step", "max_cost_micro_usd", "committed_micro_usd", "outstanding_micro_usd", "max_runs", "evidence_expires_at", "latest_wake_id", "proposal_artifact_id", "failure_code", "runs", "outcomes"]

    @field_validator('status')
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['waiting', 'running', 'needs_investigation', 'publishing', 'proposal', 'stopped', 'closed']):
            raise ValueError("must be one of enum values ('waiting', 'running', 'needs_investigation', 'publishing', 'proposal', 'stopped', 'closed')")
        return value

    @field_validator('max_cost_micro_usd', mode="before")
    def max_cost_micro_usd_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if isinstance(value, str) and not re.match(r"^[0-9]{1,12}$", value):
            raise ValueError(r"must validate the regular expression /^[0-9]{1,12}$/")
        return value

    @field_validator('committed_micro_usd', mode="before")
    def committed_micro_usd_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if isinstance(value, str) and not re.match(r"^[0-9]{1,12}$", value):
            raise ValueError(r"must validate the regular expression /^[0-9]{1,12}$/")
        return value

    @field_validator('outstanding_micro_usd', mode="before")
    def outstanding_micro_usd_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if isinstance(value, str) and not re.match(r"^[0-9]{1,12}$", value):
            raise ValueError(r"must validate the regular expression /^[0-9]{1,12}$/")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of DecisionTask from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in runs (list)
        _items = []
        if self.runs:
            for _item_runs in self.runs:
                if _item_runs:
                    _items.append(_item_runs.to_dict())
            _dict['runs'] = _items
        # override the default output from pydantic by calling `to_dict()` of each item in outcomes (list)
        _items = []
        if self.outcomes:
            for _item_outcomes in self.outcomes:
                if _item_outcomes:
                    _items.append(_item_outcomes.to_dict())
            _dict['outcomes'] = _items
        # set to None if latest_wake_id (nullable) is None
        # and model_fields_set contains the field
        if self.latest_wake_id is None and "latest_wake_id" in self.model_fields_set:
            _dict['latest_wake_id'] = None

        # set to None if proposal_artifact_id (nullable) is None
        # and model_fields_set contains the field
        if self.proposal_artifact_id is None and "proposal_artifact_id" in self.model_fields_set:
            _dict['proposal_artifact_id'] = None

        # set to None if failure_code (nullable) is None
        # and model_fields_set contains the field
        if self.failure_code is None and "failure_code" in self.model_fields_set:
            _dict['failure_code'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of DecisionTask from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "workspace_id": obj.get("workspace_id"),
            "objective": obj.get("objective"),
            "status": obj.get("status"),
            "next_step": obj.get("next_step"),
            "max_cost_micro_usd": obj.get("max_cost_micro_usd"),
            "committed_micro_usd": obj.get("committed_micro_usd"),
            "outstanding_micro_usd": obj.get("outstanding_micro_usd"),
            "max_runs": obj.get("max_runs"),
            "evidence_expires_at": obj.get("evidence_expires_at"),
            "latest_wake_id": obj.get("latest_wake_id"),
            "proposal_artifact_id": obj.get("proposal_artifact_id"),
            "failure_code": obj.get("failure_code"),
            "runs": [TaskRun.from_dict(_item) for _item in obj["runs"]] if obj.get("runs") is not None else None,
            "outcomes": [TaskOutcomeReceipt.from_dict(_item) for _item in obj["outcomes"]] if obj.get("outcomes") is not None else None
        })
        return _obj


