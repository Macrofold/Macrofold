# coding: utf-8

"""
    Macrofold API

    Manage persistent workspaces, run cloud agents, stream progress, and integrate tools, billing, and operator reporting. Provider-owned OAuth, internal runtime ingress, and MCP JSON-RPC use separate contracts.

    The version of the OpenAPI document: 0.9.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from macrofold.models.context_audience import ContextAudience
from macrofold.models.context_item import ContextItem
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class ExplicitContext(BaseModel):
    """
    ExplicitContext
    """ # noqa: E501
    schema_version: StrictInt
    template_revision: Annotated[str, Field(min_length=1, strict=True, max_length=200)]
    audience: ContextAudience
    items: Annotated[List[ContextItem], Field(max_length=64)]
    complete: StrictBool
    truncated: StrictBool
    consistency: StrictStr
    observed_at: datetime
    read_completed_at: Optional[datetime] = None
    expires_at: Optional[datetime] = None
    dependency_tokens: Dict[str, Annotated[str, Field(min_length=1, strict=True, max_length=512)]]
    __properties: ClassVar[List[str]] = ["schema_version", "template_revision", "audience", "items", "complete", "truncated", "consistency", "observed_at", "read_completed_at", "expires_at", "dependency_tokens"]

    @field_validator('schema_version')
    def schema_version_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set([1]):
            raise ValueError("must be one of enum values (1)")
        return value

    @field_validator('consistency')
    def consistency_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['snapshot', 'read_interval']):
            raise ValueError("must be one of enum values ('snapshot', 'read_interval')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ExplicitContext from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of audience
        if self.audience:
            _dict['audience'] = self.audience.to_dict()
        # override the default output from pydantic by calling `to_dict()` of each item in items (list)
        _items = []
        if self.items:
            for _item_items in self.items:
                if _item_items:
                    _items.append(_item_items.to_dict())
            _dict['items'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ExplicitContext from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "schema_version": obj.get("schema_version"),
            "template_revision": obj.get("template_revision"),
            "audience": ContextAudience.from_dict(obj["audience"]) if obj.get("audience") is not None else None,
            "items": [ContextItem.from_dict(_item) for _item in obj["items"]] if obj.get("items") is not None else None,
            "complete": obj.get("complete"),
            "truncated": obj.get("truncated"),
            "consistency": obj.get("consistency"),
            "observed_at": obj.get("observed_at"),
            "read_completed_at": obj.get("read_completed_at"),
            "expires_at": obj.get("expires_at"),
            "dependency_tokens": obj.get("dependency_tokens")
        })
        return _obj


