# coding: utf-8

"""
    Macrofold API

    Manage persistent projects, run cloud agents, stream progress, and integrate tools, billing, and operator reporting. Provider-owned OAuth, internal runtime ingress, and MCP JSON-RPC use separate contracts.

    The version of the OpenAPI document: 0.9.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from uuid import UUID
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class GitSync(BaseModel):
    """
    GitSync
    """ # noqa: E501
    workspace_id: UUID
    status: StrictStr
    source_commit: Optional[StrictStr] = None
    target_commit: Optional[StrictStr] = None
    conflicting_paths: Optional[List[StrictStr]] = None
    error_code: Optional[StrictStr] = None
    updated_at: datetime
    pull_request_url: Optional[StrictStr] = None
    __properties: ClassVar[List[str]] = ["workspace_id", "status", "source_commit", "target_commit", "conflicting_paths", "error_code", "updated_at", "pull_request_url"]

    @field_validator('status')
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['disabled', 'pending', 'running', 'synced', 'already_integrated', 'conflict', 'blocked', 'failed']):
            raise ValueError("must be one of enum values ('disabled', 'pending', 'running', 'synced', 'already_integrated', 'conflict', 'blocked', 'failed')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of GitSync from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of GitSync from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "workspace_id": obj.get("workspace_id"),
            "status": obj.get("status"),
            "source_commit": obj.get("source_commit"),
            "target_commit": obj.get("target_commit"),
            "conflicting_paths": obj.get("conflicting_paths"),
            "error_code": obj.get("error_code"),
            "updated_at": obj.get("updated_at"),
            "pull_request_url": obj.get("pull_request_url")
        })
        return _obj


