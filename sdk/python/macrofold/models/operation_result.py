# coding: utf-8

"""
    Macrofold API

    Manage persistent projects, run cloud agents, stream progress, and integrate tools, billing, and operator reporting. Provider-owned OAuth, internal runtime ingress, and MCP JSON-RPC use separate contracts.

    The version of the OpenAPI document: 0.9.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from uuid import UUID
from macrofold.models.file_entry import FileEntry
from macrofold.models.git_sync import GitSync
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class OperationResult(BaseModel):
    """
    Versioned metadata; secrets and unbounded arbitrary payloads are prohibited.
    """ # noqa: E501
    workspace_id: Optional[UUID] = None
    revision: Optional[StrictStr] = None
    checkpoint_id: Optional[UUID] = None
    path: Optional[StrictStr] = Field(default=None, description="Created, saved, renamed, or removed path for a file mutation.")
    previous_path: Optional[StrictStr] = Field(default=None, description="Original path for a file rename.")
    entry: Optional[FileEntry] = Field(default=None, description="Authoritative resulting file or directory entry, absent for deletion.")
    project_id: Optional[UUID] = None
    transfer_id: Optional[UUID] = None
    delivery_id: Optional[UUID] = None
    local_receipt_complete: Optional[StrictBool] = None
    format: Optional[StrictStr] = None
    download_url: Optional[StrictStr] = None
    manifest_url: Optional[StrictStr] = None
    expires_at: Optional[datetime] = None
    size_bytes: Optional[Annotated[str, Field(strict=True)]] = None
    sha256: Optional[Annotated[str, Field(strict=True)]] = None
    manifest_sha256: Optional[Annotated[str, Field(strict=True)]] = None
    export_commit: Optional[StrictStr] = None
    source_commit: Optional[StrictStr] = None
    sync: Optional[GitSync] = None
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["workspace_id", "revision", "checkpoint_id", "path", "previous_path", "entry", "project_id", "transfer_id", "delivery_id", "local_receipt_complete", "format", "download_url", "manifest_url", "expires_at", "size_bytes", "sha256", "manifest_sha256", "export_commit", "source_commit", "sync"]

    @field_validator('format')
    def format_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['git_bundle', 'portable_archive']):
            raise ValueError("must be one of enum values ('git_bundle', 'portable_archive')")
        return value

    @field_validator('size_bytes', mode="before")
    def size_bytes_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if isinstance(value, str) and not re.match(r"^[0-9]+$", value):
            raise ValueError(r"must validate the regular expression /^[0-9]+$/")
        return value

    @field_validator('sha256', mode="before")
    def sha256_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if isinstance(value, str) and not re.match(r"^[a-f0-9]{64}$", value):
            raise ValueError(r"must validate the regular expression /^[a-f0-9]{64}$/")
        return value

    @field_validator('manifest_sha256', mode="before")
    def manifest_sha256_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if isinstance(value, str) and not re.match(r"^[a-f0-9]{64}$", value):
            raise ValueError(r"must validate the regular expression /^[a-f0-9]{64}$/")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of OperationResult from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of entry
        if self.entry:
            _dict['entry'] = self.entry.to_dict()
        # override the default output from pydantic by calling `to_dict()` of sync
        if self.sync:
            _dict['sync'] = self.sync.to_dict()
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of OperationResult from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "workspace_id": obj.get("workspace_id"),
            "revision": obj.get("revision"),
            "checkpoint_id": obj.get("checkpoint_id"),
            "path": obj.get("path"),
            "previous_path": obj.get("previous_path"),
            "entry": FileEntry.from_dict(obj["entry"]) if obj.get("entry") is not None else None,
            "project_id": obj.get("project_id"),
            "transfer_id": obj.get("transfer_id"),
            "delivery_id": obj.get("delivery_id"),
            "local_receipt_complete": obj.get("local_receipt_complete"),
            "format": obj.get("format"),
            "download_url": obj.get("download_url"),
            "manifest_url": obj.get("manifest_url"),
            "expires_at": obj.get("expires_at"),
            "size_bytes": obj.get("size_bytes"),
            "sha256": obj.get("sha256"),
            "manifest_sha256": obj.get("manifest_sha256"),
            "export_commit": obj.get("export_commit"),
            "source_commit": obj.get("source_commit"),
            "sync": GitSync.from_dict(obj["sync"]) if obj.get("sync") is not None else None
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


