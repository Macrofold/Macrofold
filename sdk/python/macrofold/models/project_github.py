# coding: utf-8

"""
    Macrofold API

    Manage persistent projects, run cloud agents, stream progress, and integrate tools, billing, and operator reporting. Provider-owned OAuth, internal runtime ingress, and MCP JSON-RPC use separate contracts.

    The version of the OpenAPI document: 0.9.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class ProjectGithub(BaseModel):
    """
    ProjectGithub
    """ # noqa: E501
    installation_id: StrictStr
    repository_id: StrictStr
    target_branch: StrictStr
    auto_sync: Optional[StrictBool] = Field(default=False, description="Opt in to synchronizing after persisted agent runs.")
    sync_mode: Optional[StrictStr] = 'push'
    auto_pull: Optional[StrictBool] = Field(default=False, description="Fetch and merge incoming target-branch pushes when the workspace is idle. Conflicts preserve local files.")
    __properties: ClassVar[List[str]] = ["installation_id", "repository_id", "target_branch", "auto_sync", "sync_mode", "auto_pull"]

    @field_validator('sync_mode')
    def sync_mode_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['push', 'pull_request']):
            raise ValueError("must be one of enum values ('push', 'pull_request')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ProjectGithub from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ProjectGithub from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "installation_id": obj.get("installation_id"),
            "repository_id": obj.get("repository_id"),
            "target_branch": obj.get("target_branch"),
            "auto_sync": obj.get("auto_sync") if obj.get("auto_sync") is not None else False,
            "sync_mode": obj.get("sync_mode") if obj.get("sync_mode") is not None else 'push',
            "auto_pull": obj.get("auto_pull") if obj.get("auto_pull") is not None else False
        })
        return _obj


