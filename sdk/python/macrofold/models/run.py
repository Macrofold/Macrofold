# coding: utf-8

"""
    Macrofold API

    Manage persistent projects, run cloud agents, stream progress, and integrate tools, billing, and operator reporting. Provider-owned OAuth, internal runtime ingress, and MCP JSON-RPC use separate contracts.

    The version of the OpenAPI document: 0.9.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional, Union
from typing_extensions import Annotated
from uuid import UUID
from macrofold.models.limits import Limits
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class Run(BaseModel):
    """
    Run
    """ # noqa: E501
    id: UUID
    organization_id: UUID
    session_id: UUID
    workspace_id: UUID
    harness: StrictStr
    model: StrictStr
    status: StrictStr
    execution_outcome: Optional[StrictStr] = None
    persistence_status: Optional[StrictStr] = None
    sync_status: Optional[StrictStr] = None
    created_at: datetime
    started_at: Optional[datetime] = None
    completed_at: Optional[datetime] = None
    limits: Optional[Limits] = None
    cost_micro_usd: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="Integer micro-USD as a decimal string.")
    queue_expires_at: Optional[datetime] = None
    failure_code: Optional[StrictStr] = None
    client_type: Optional[StrictStr] = None
    client_version: Optional[StrictStr] = None
    wait_seconds: Optional[Union[Annotated[float, Field(strict=True, ge=0)], Annotated[int, Field(strict=True, ge=0)]]] = Field(default=None, description="Submission to execution start, or elapsed wait through now/completion if never started.")
    waiting_reason: Optional[StrictStr] = Field(default=None, description="Snapshot, not a queue position or start-time estimate. Null when no longer queued.")
    reserved_micro_usd: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="Funds still held and unavailable for other jobs; released on settlement.")
    scheduling_class: Optional[StrictStr] = None
    execution_deadline: Optional[datetime] = Field(default=None, description="Set when execution is claimed. Includes provisioning and persistence; separate from the queue deadline.")
    __properties: ClassVar[List[str]] = ["id", "organization_id", "session_id", "workspace_id", "harness", "model", "status", "execution_outcome", "persistence_status", "sync_status", "created_at", "started_at", "completed_at", "limits", "cost_micro_usd", "queue_expires_at", "failure_code", "client_type", "client_version", "wait_seconds", "waiting_reason", "reserved_micro_usd", "scheduling_class", "execution_deadline"]

    @field_validator('status')
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['queued', 'provisioning', 'running', 'waiting_for_input', 'persisting', 'succeeded', 'failed', 'cancelled', 'timed_out']):
            raise ValueError("must be one of enum values ('queued', 'provisioning', 'running', 'waiting_for_input', 'persisting', 'succeeded', 'failed', 'cancelled', 'timed_out')")
        return value

    @field_validator('execution_outcome')
    def execution_outcome_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['pending', 'success', 'failure', 'cancelled', 'timed_out', 'unknown']):
            raise ValueError("must be one of enum values ('pending', 'success', 'failure', 'cancelled', 'timed_out', 'unknown')")
        return value

    @field_validator('persistence_status')
    def persistence_status_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['pending', 'verified', 'failed', 'not_required']):
            raise ValueError("must be one of enum values ('pending', 'verified', 'failed', 'not_required')")
        return value

    @field_validator('sync_status')
    def sync_status_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['disabled', 'pending', 'synced', 'conflict', 'blocked', 'failed']):
            raise ValueError("must be one of enum values ('disabled', 'pending', 'synced', 'conflict', 'blocked', 'failed')")
        return value

    @field_validator('cost_micro_usd', mode="before")
    def cost_micro_usd_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if isinstance(value, str) and not re.match(r"^[0-9]+$", value):
            raise ValueError(r"must validate the regular expression /^[0-9]+$/")
        return value

    @field_validator('client_type')
    def client_type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['dashboard', 'cli', 'sdk', 'api', 'internal']):
            raise ValueError("must be one of enum values ('dashboard', 'cli', 'sdk', 'api', 'internal')")
        return value

    @field_validator('waiting_reason')
    def waiting_reason_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['global_capacity', 'account_concurrency', 'earlier_workspace_work', 'scheduler_turn', 'cancellation_requested', 'deadline_expired', 'workspace_unavailable']):
            raise ValueError("must be one of enum values ('global_capacity', 'account_concurrency', 'earlier_workspace_work', 'scheduler_turn', 'cancellation_requested', 'deadline_expired', 'workspace_unavailable')")
        return value

    @field_validator('reserved_micro_usd', mode="before")
    def reserved_micro_usd_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if isinstance(value, str) and not re.match(r"^[0-9]+$", value):
            raise ValueError(r"must validate the regular expression /^[0-9]+$/")
        return value

    @field_validator('scheduling_class')
    def scheduling_class_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['background', 'interactive']):
            raise ValueError("must be one of enum values ('background', 'interactive')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Run from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of limits
        if self.limits:
            _dict['limits'] = self.limits.to_dict()
        # set to None if waiting_reason (nullable) is None
        # and model_fields_set contains the field
        if self.waiting_reason is None and "waiting_reason" in self.model_fields_set:
            _dict['waiting_reason'] = None

        # set to None if execution_deadline (nullable) is None
        # and model_fields_set contains the field
        if self.execution_deadline is None and "execution_deadline" in self.model_fields_set:
            _dict['execution_deadline'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Run from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "organization_id": obj.get("organization_id"),
            "session_id": obj.get("session_id"),
            "workspace_id": obj.get("workspace_id"),
            "harness": obj.get("harness"),
            "model": obj.get("model"),
            "status": obj.get("status"),
            "execution_outcome": obj.get("execution_outcome"),
            "persistence_status": obj.get("persistence_status"),
            "sync_status": obj.get("sync_status"),
            "created_at": obj.get("created_at"),
            "started_at": obj.get("started_at"),
            "completed_at": obj.get("completed_at"),
            "limits": Limits.from_dict(obj["limits"]) if obj.get("limits") is not None else None,
            "cost_micro_usd": obj.get("cost_micro_usd"),
            "queue_expires_at": obj.get("queue_expires_at"),
            "failure_code": obj.get("failure_code"),
            "client_type": obj.get("client_type"),
            "client_version": obj.get("client_version"),
            "wait_seconds": obj.get("wait_seconds"),
            "waiting_reason": obj.get("waiting_reason"),
            "reserved_micro_usd": obj.get("reserved_micro_usd"),
            "scheduling_class": obj.get("scheduling_class"),
            "execution_deadline": obj.get("execution_deadline")
        })
        return _obj


