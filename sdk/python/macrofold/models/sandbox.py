# coding: utf-8

"""
    Macrofold API

    Manage persistent workspaces, run cloud agents, stream progress, and integrate tools, billing, and operator reporting. Provider-owned OAuth, internal runtime ingress, and MCP JSON-RPC use separate contracts.

    The version of the OpenAPI document: 0.9.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from uuid import UUID
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class Sandbox(BaseModel):
    """
    Sandbox
    """ # noqa: E501
    id: UUID
    workspace_id: UUID
    worktree_id: UUID
    name: Optional[StrictStr]
    long_running: StrictBool
    keep_warm_seconds: Optional[Annotated[int, Field(le=86400, strict=True, ge=0)]] = Field(description="Seconds to retain idle compute after a run. 0 or null on a run releases compute. Omitted inherits the sandbox policy.")
    status: StrictStr
    active_run_id: Optional[UUID]
    idle_expires_at: Optional[datetime]
    cost_micro_usd: Annotated[str, Field(strict=True)]
    max_cost_micro_usd: Annotated[str, Field(strict=True)]
    reserved_micro_usd: Annotated[str, Field(strict=True)]
    rate_micro_usd_per_minute: Annotated[str, Field(strict=True)]
    failure_code: Optional[StrictStr]
    created_at: datetime
    updated_at: datetime
    __properties: ClassVar[List[str]] = ["id", "workspace_id", "worktree_id", "name", "long_running", "keep_warm_seconds", "status", "active_run_id", "idle_expires_at", "cost_micro_usd", "max_cost_micro_usd", "reserved_micro_usd", "rate_micro_usd_per_minute", "failure_code", "created_at", "updated_at"]

    @field_validator('status')
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['creating', 'ready', 'pausing', 'paused', 'destroying', 'destroyed', 'error']):
            raise ValueError("must be one of enum values ('creating', 'ready', 'pausing', 'paused', 'destroying', 'destroyed', 'error')")
        return value

    @field_validator('cost_micro_usd', mode="before")
    def cost_micro_usd_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if isinstance(value, str) and not re.match(r"^[0-9]{1,12}$", value):
            raise ValueError(r"must validate the regular expression /^[0-9]{1,12}$/")
        return value

    @field_validator('max_cost_micro_usd', mode="before")
    def max_cost_micro_usd_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if isinstance(value, str) and not re.match(r"^[0-9]{1,12}$", value):
            raise ValueError(r"must validate the regular expression /^[0-9]{1,12}$/")
        return value

    @field_validator('reserved_micro_usd', mode="before")
    def reserved_micro_usd_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if isinstance(value, str) and not re.match(r"^[0-9]{1,12}$", value):
            raise ValueError(r"must validate the regular expression /^[0-9]{1,12}$/")
        return value

    @field_validator('rate_micro_usd_per_minute', mode="before")
    def rate_micro_usd_per_minute_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if isinstance(value, str) and not re.match(r"^[0-9]{1,12}$", value):
            raise ValueError(r"must validate the regular expression /^[0-9]{1,12}$/")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Sandbox from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if name (nullable) is None
        # and model_fields_set contains the field
        if self.name is None and "name" in self.model_fields_set:
            _dict['name'] = None

        # set to None if keep_warm_seconds (nullable) is None
        # and model_fields_set contains the field
        if self.keep_warm_seconds is None and "keep_warm_seconds" in self.model_fields_set:
            _dict['keep_warm_seconds'] = None

        # set to None if active_run_id (nullable) is None
        # and model_fields_set contains the field
        if self.active_run_id is None and "active_run_id" in self.model_fields_set:
            _dict['active_run_id'] = None

        # set to None if idle_expires_at (nullable) is None
        # and model_fields_set contains the field
        if self.idle_expires_at is None and "idle_expires_at" in self.model_fields_set:
            _dict['idle_expires_at'] = None

        # set to None if failure_code (nullable) is None
        # and model_fields_set contains the field
        if self.failure_code is None and "failure_code" in self.model_fields_set:
            _dict['failure_code'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Sandbox from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "workspace_id": obj.get("workspace_id"),
            "worktree_id": obj.get("worktree_id"),
            "name": obj.get("name"),
            "long_running": obj.get("long_running"),
            "keep_warm_seconds": obj.get("keep_warm_seconds"),
            "status": obj.get("status"),
            "active_run_id": obj.get("active_run_id"),
            "idle_expires_at": obj.get("idle_expires_at"),
            "cost_micro_usd": obj.get("cost_micro_usd"),
            "max_cost_micro_usd": obj.get("max_cost_micro_usd"),
            "reserved_micro_usd": obj.get("reserved_micro_usd"),
            "rate_micro_usd_per_minute": obj.get("rate_micro_usd_per_minute"),
            "failure_code": obj.get("failure_code"),
            "created_at": obj.get("created_at"),
            "updated_at": obj.get("updated_at")
        })
        return _obj


