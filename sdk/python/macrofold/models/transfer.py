# coding: utf-8

"""
    Macrofold API

    Manage persistent projects, run cloud agents, stream progress, and integrate tools, billing, and operator reporting. Provider-owned OAuth, internal runtime ingress, and MCP JSON-RPC use separate contracts.

    The version of the OpenAPI document: 0.9.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, StrictBool, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from uuid import UUID
from macrofold.models.transfer_action import TransferAction
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class Transfer(BaseModel):
    """
    Transfer
    """ # noqa: E501
    id: UUID
    workspace_id: UUID
    direction: StrictStr
    base_revision: StrictStr
    status: StrictStr
    dry_run: StrictBool
    actions: List[TransferAction]
    expires_at: datetime
    created_at: datetime
    operation_id: Optional[UUID] = None
    result_revision: Optional[StrictStr] = None
    checkpoint_id: Optional[UUID] = None
    local_receipt_complete: Optional[StrictBool] = None
    __properties: ClassVar[List[str]] = ["id", "workspace_id", "direction", "base_revision", "status", "dry_run", "actions", "expires_at", "created_at", "operation_id", "result_revision", "checkpoint_id", "local_receipt_complete"]

    @field_validator('direction')
    def direction_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['push', 'pull']):
            raise ValueError("must be one of enum values ('push', 'pull')")
        return value

    @field_validator('status')
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['planned', 'staging', 'ready', 'applying', 'succeeded', 'conflicted', 'expired', 'failed']):
            raise ValueError("must be one of enum values ('planned', 'staging', 'ready', 'applying', 'succeeded', 'conflicted', 'expired', 'failed')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Transfer from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in actions (list)
        _items = []
        if self.actions:
            for _item_actions in self.actions:
                if _item_actions:
                    _items.append(_item_actions.to_dict())
            _dict['actions'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Transfer from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "workspace_id": obj.get("workspace_id"),
            "direction": obj.get("direction"),
            "base_revision": obj.get("base_revision"),
            "status": obj.get("status"),
            "dry_run": obj.get("dry_run"),
            "actions": [TransferAction.from_dict(_item) for _item in obj["actions"]] if obj.get("actions") is not None else None,
            "expires_at": obj.get("expires_at"),
            "created_at": obj.get("created_at"),
            "operation_id": obj.get("operation_id"),
            "result_revision": obj.get("result_revision"),
            "checkpoint_id": obj.get("checkpoint_id"),
            "local_receipt_complete": obj.get("local_receipt_complete")
        })
        return _obj


