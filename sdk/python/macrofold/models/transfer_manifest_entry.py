# coding: utf-8

"""
    Macrofold API

    Manage persistent projects, run cloud agents, stream progress, and integrate tools, billing, and operator reporting. Provider-owned OAuth, internal runtime ingress, and MCP JSON-RPC use separate contracts.

    The version of the OpenAPI document: 0.9.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class TransferManifestEntry(BaseModel):
    """
    Explicit absent local file has null hash and zero bytes. baseline_known=false is distinct from a known absent baseline. Regular-file transfers only; reject symlink/device/directory conflicts. Runtime validates unique normalized paths and consistent hashes/sizes.
    """ # noqa: E501
    path: Annotated[str, Field(min_length=1, strict=True, max_length=4096)] = Field(description="Normalized relative POSIX path; runtime rejects traversal, NUL, absolute paths, unsafe links and reserved internal paths.")
    local_sha256: Optional[Annotated[str, Field(strict=True)]] = Field(description="SHA-256 of a regular file; null means absent.")
    local_size_bytes: Annotated[int, Field(le=26214400, strict=True, ge=0)]
    baseline_known: StrictBool
    baseline_sha256: Optional[Annotated[str, Field(strict=True)]] = Field(description="SHA-256 of a regular file; null means absent.")
    __properties: ClassVar[List[str]] = ["path", "local_sha256", "local_size_bytes", "baseline_known", "baseline_sha256"]

    @field_validator('local_sha256', mode="before")
    def local_sha256_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if isinstance(value, str) and not re.match(r"^[a-f0-9]{64}$", value):
            raise ValueError(r"must validate the regular expression /^[a-f0-9]{64}$/")
        return value

    @field_validator('baseline_sha256', mode="before")
    def baseline_sha256_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if isinstance(value, str) and not re.match(r"^[a-f0-9]{64}$", value):
            raise ValueError(r"must validate the regular expression /^[a-f0-9]{64}$/")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of TransferManifestEntry from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if local_sha256 (nullable) is None
        # and model_fields_set contains the field
        if self.local_sha256 is None and "local_sha256" in self.model_fields_set:
            _dict['local_sha256'] = None

        # set to None if baseline_sha256 (nullable) is None
        # and model_fields_set contains the field
        if self.baseline_sha256 is None and "baseline_sha256" in self.model_fields_set:
            _dict['baseline_sha256'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of TransferManifestEntry from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "path": obj.get("path"),
            "local_sha256": obj.get("local_sha256"),
            "local_size_bytes": obj.get("local_size_bytes"),
            "baseline_known": obj.get("baseline_known"),
            "baseline_sha256": obj.get("baseline_sha256")
        })
        return _obj


