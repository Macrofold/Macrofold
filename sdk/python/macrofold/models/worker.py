# coding: utf-8

"""
    Macrofold API

    Manage persistent workspaces, run cloud agents, stream progress, and integrate tools, billing, and operator reporting. Provider-owned OAuth, internal runtime ingress, and MCP JSON-RPC use separate contracts.

    The version of the OpenAPI document: 0.9.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from uuid import UUID
from macrofold.models.worker_offering import WorkerOffering
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class Worker(BaseModel):
    """
    Worker
    """ # noqa: E501
    name: Optional[StrictStr]
    compute: StrictStr = Field(description="Economic offering, not a placement hint. Macrofold never silently switches this choice.")
    dedicated: StrictBool = Field(description="Exclusive capacity for this Worker. Independent of sibling Run isolation.")
    isolate_runs: StrictBool = Field(description="Require the advertised boundary between sibling Runs. False permits trusted sharing only within this Worker.")
    region: Annotated[str, Field(min_length=1, strict=True, max_length=160)]
    runtime: Annotated[str, Field(min_length=1, strict=True, max_length=160)]
    size: Optional[StrictStr]
    min_instances: Annotated[int, Field(le=64, strict=True, ge=0)]
    max_instances: Optional[Annotated[int, Field(strict=True, ge=1)]]
    max_concurrency: Annotated[int, Field(le=1024, strict=True, ge=1)]
    idle_timeout_seconds: Optional[Annotated[int, Field(le=86400, strict=True, ge=0)]] = Field(description="Idle scale-down grace. Null retains idle dedicated capacity while enabled and funded.")
    expires_at: Optional[datetime]
    max_hourly_compute_cost_micro_usd: Annotated[str, Field(strict=True)] = Field(description="Maximum aggregate committed compute rate, including starting and draining allocations. This is not a model budget or a daily spending budget.")
    id: UUID
    organization_id: UUID
    revision: Annotated[int, Field(strict=True, ge=1)]
    desired_state: StrictStr
    status: StrictStr
    active_runs: Annotated[int, Field(strict=True, ge=0)]
    occupied_slots: Annotated[int, Field(strict=True, ge=0)]
    queued_runs: Annotated[int, Field(strict=True, ge=0)]
    ready_instances: Annotated[int, Field(strict=True, ge=0)]
    starting_instances: Annotated[int, Field(strict=True, ge=0)]
    draining_instances: Annotated[int, Field(strict=True, ge=0)]
    committed_hourly_compute_cost_micro_usd: Annotated[str, Field(strict=True)]
    cost_micro_usd: Annotated[str, Field(strict=True)]
    reserved_micro_usd: Annotated[str, Field(strict=True)]
    accepted_offerings: List[WorkerOffering]
    failure_code: Optional[StrictStr]
    created_at: datetime
    updated_at: datetime
    observed_at: datetime
    __properties: ClassVar[List[str]] = ["name", "compute", "dedicated", "isolate_runs", "region", "runtime", "size", "min_instances", "max_instances", "max_concurrency", "idle_timeout_seconds", "expires_at", "max_hourly_compute_cost_micro_usd", "id", "organization_id", "revision", "desired_state", "status", "active_runs", "occupied_slots", "queued_runs", "ready_instances", "starting_instances", "draining_instances", "committed_hourly_compute_cost_micro_usd", "cost_micro_usd", "reserved_micro_usd", "accepted_offerings", "failure_code", "created_at", "updated_at", "observed_at"]

    @field_validator('compute')
    def compute_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['server', 'sandbox']):
            raise ValueError("must be one of enum values ('server', 'sandbox')")
        return value

    @field_validator('max_hourly_compute_cost_micro_usd', mode="before")
    def max_hourly_compute_cost_micro_usd_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if isinstance(value, str) and not re.match(r"^(0|[1-9][0-9]{0,14})$", value):
            raise ValueError(r"must validate the regular expression /^(0|[1-9][0-9]{0,14})$/")
        return value

    @field_validator('desired_state')
    def desired_state_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['enabled', 'paused', 'destroyed']):
            raise ValueError("must be one of enum values ('enabled', 'paused', 'destroyed')")
        return value

    @field_validator('status')
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['sleeping', 'starting', 'ready', 'draining', 'paused', 'destroyed', 'expired']):
            raise ValueError("must be one of enum values ('sleeping', 'starting', 'ready', 'draining', 'paused', 'destroyed', 'expired')")
        return value

    @field_validator('committed_hourly_compute_cost_micro_usd', mode="before")
    def committed_hourly_compute_cost_micro_usd_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if isinstance(value, str) and not re.match(r"^(0|[1-9][0-9]{0,14})$", value):
            raise ValueError(r"must validate the regular expression /^(0|[1-9][0-9]{0,14})$/")
        return value

    @field_validator('cost_micro_usd', mode="before")
    def cost_micro_usd_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if isinstance(value, str) and not re.match(r"^[0-9]+$", value):
            raise ValueError(r"must validate the regular expression /^[0-9]+$/")
        return value

    @field_validator('reserved_micro_usd', mode="before")
    def reserved_micro_usd_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if isinstance(value, str) and not re.match(r"^[0-9]+$", value):
            raise ValueError(r"must validate the regular expression /^[0-9]+$/")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Worker from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in accepted_offerings (list)
        _items = []
        if self.accepted_offerings:
            for _item_accepted_offerings in self.accepted_offerings:
                if _item_accepted_offerings:
                    _items.append(_item_accepted_offerings.to_dict())
            _dict['accepted_offerings'] = _items
        # set to None if name (nullable) is None
        # and model_fields_set contains the field
        if self.name is None and "name" in self.model_fields_set:
            _dict['name'] = None

        # set to None if size (nullable) is None
        # and model_fields_set contains the field
        if self.size is None and "size" in self.model_fields_set:
            _dict['size'] = None

        # set to None if max_instances (nullable) is None
        # and model_fields_set contains the field
        if self.max_instances is None and "max_instances" in self.model_fields_set:
            _dict['max_instances'] = None

        # set to None if idle_timeout_seconds (nullable) is None
        # and model_fields_set contains the field
        if self.idle_timeout_seconds is None and "idle_timeout_seconds" in self.model_fields_set:
            _dict['idle_timeout_seconds'] = None

        # set to None if expires_at (nullable) is None
        # and model_fields_set contains the field
        if self.expires_at is None and "expires_at" in self.model_fields_set:
            _dict['expires_at'] = None

        # set to None if failure_code (nullable) is None
        # and model_fields_set contains the field
        if self.failure_code is None and "failure_code" in self.model_fields_set:
            _dict['failure_code'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Worker from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "name": obj.get("name"),
            "compute": obj.get("compute"),
            "dedicated": obj.get("dedicated"),
            "isolate_runs": obj.get("isolate_runs"),
            "region": obj.get("region"),
            "runtime": obj.get("runtime"),
            "size": obj.get("size"),
            "min_instances": obj.get("min_instances"),
            "max_instances": obj.get("max_instances"),
            "max_concurrency": obj.get("max_concurrency"),
            "idle_timeout_seconds": obj.get("idle_timeout_seconds"),
            "expires_at": obj.get("expires_at"),
            "max_hourly_compute_cost_micro_usd": obj.get("max_hourly_compute_cost_micro_usd"),
            "id": obj.get("id"),
            "organization_id": obj.get("organization_id"),
            "revision": obj.get("revision"),
            "desired_state": obj.get("desired_state"),
            "status": obj.get("status"),
            "active_runs": obj.get("active_runs"),
            "occupied_slots": obj.get("occupied_slots"),
            "queued_runs": obj.get("queued_runs"),
            "ready_instances": obj.get("ready_instances"),
            "starting_instances": obj.get("starting_instances"),
            "draining_instances": obj.get("draining_instances"),
            "committed_hourly_compute_cost_micro_usd": obj.get("committed_hourly_compute_cost_micro_usd"),
            "cost_micro_usd": obj.get("cost_micro_usd"),
            "reserved_micro_usd": obj.get("reserved_micro_usd"),
            "accepted_offerings": [WorkerOffering.from_dict(_item) for _item in obj["accepted_offerings"]] if obj.get("accepted_offerings") is not None else None,
            "failure_code": obj.get("failure_code"),
            "created_at": obj.get("created_at"),
            "updated_at": obj.get("updated_at"),
            "observed_at": obj.get("observed_at")
        })
        return _obj


