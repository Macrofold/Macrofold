/*
 * Macrofold API
 *
 * Manage persistent workspaces, run cloud agents, stream progress, and integrate tools, billing, and operator reporting. Provider-owned OAuth, internal runtime ingress, and MCP JSON-RPC use separate contracts.
 *
 * The version of the OpenAPI document: 0.9.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// CustomerConnectionAuthorization : Short-lived setup link, not a platform API credential. Open it in a popup or top-level window; its fragment must not be logged. Expired or uncertain attempts require a newly requested link.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct CustomerConnectionAuthorization {
    #[serde(rename = "authorization_id")]
    pub authorization_id: uuid::Uuid,
    #[serde(rename = "authorization_url")]
    pub authorization_url: String,
    #[serde(rename = "expires_at")]
    pub expires_at: chrono::DateTime<chrono::FixedOffset>,
}

impl CustomerConnectionAuthorization {
    /// Short-lived setup link, not a platform API credential. Open it in a popup or top-level window; its fragment must not be logged. Expired or uncertain attempts require a newly requested link.
    pub fn new(authorization_id: uuid::Uuid, authorization_url: String, expires_at: chrono::DateTime<chrono::FixedOffset>) -> CustomerConnectionAuthorization {
        CustomerConnectionAuthorization {
            authorization_id,
            authorization_url,
            expires_at,
        }
    }
}

