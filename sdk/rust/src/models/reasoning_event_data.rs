/*
 * Macrofold API
 *
 * Manage persistent workspaces, run cloud agents, stream progress, and integrate tools, billing, and operator reporting. Provider-owned OAuth, internal runtime ingress, and MCP JSON-RPC use separate contracts.
 *
 * The version of the OpenAPI document: 0.9.0
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// ReasoningEventData : Native reasoning.started/delta/completed payload. Provider-exposed readable reasoning only; separate from output.delta and final output_text. Append delta text by reasoning_id in event sequence order. Completion closes a block, not the run. A terminal run without block completion means interruption.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct ReasoningEventData {
    /// Stable block identity within this run; distinct across model turns.
    #[serde(rename = "reasoning_id")]
    pub reasoning_id: String,
    /// Provider summary or provider-exposed thinking text; included on started/delta.
    #[serde(rename = "format", skip_serializing_if = "Option::is_none")]
    pub format: Option<Format>,
    /// Append-only readable fragment on reasoning.delta. Never encrypted reasoning or a signature.
    #[serde(rename = "text", skip_serializing_if = "Option::is_none")]
    pub text: Option<String>,
    /// On reasoning.completed; completed means the block ended, not that its statements are authoritative.
    #[serde(rename = "status", skip_serializing_if = "Option::is_none")]
    pub status: Option<Status>,
}

impl ReasoningEventData {
    /// Native reasoning.started/delta/completed payload. Provider-exposed readable reasoning only; separate from output.delta and final output_text. Append delta text by reasoning_id in event sequence order. Completion closes a block, not the run. A terminal run without block completion means interruption.
    pub fn new(reasoning_id: String) -> ReasoningEventData {
        ReasoningEventData {
            reasoning_id,
            format: None,
            text: None,
            status: None,
        }
    }
}
/// Provider summary or provider-exposed thinking text; included on started/delta.
#[derive(Clone, Copy, Debug, Eq, PartialEq, Ord, PartialOrd, Hash, Serialize, Deserialize)]
pub enum Format {
    #[serde(rename = "summary")]
    Summary,
    #[serde(rename = "text")]
    Text,
}

impl Default for Format {
    fn default() -> Format {
        Self::Summary
    }
}
/// On reasoning.completed; completed means the block ended, not that its statements are authoritative.
#[derive(Clone, Copy, Debug, Eq, PartialEq, Ord, PartialOrd, Hash, Serialize, Deserialize)]
pub enum Status {
    #[serde(rename = "completed")]
    Completed,
    #[serde(rename = "interrupted")]
    Interrupted,
}

impl Default for Status {
    fn default() -> Status {
        Self::Completed
    }
}

