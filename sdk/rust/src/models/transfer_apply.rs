/*
 * Macrofold API
 *
 * Manage persistent projects, run cloud agents, stream progress, and integrate tools, billing, and operator reporting. Provider-owned OAuth, internal runtime ingress, and MCP JSON-RPC use separate contracts.
 *
 * The version of the OpenAPI document: 0.9.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// TransferApply : Push: verify staged bytes, recheck revision/writer, create and checkpoint replacement, atomically switch. Pull: completed_paths is an untrusted client receipt of local work, never proof of remote mutation; the remote snapshot stays unchanged. Partial receipts remain resumable. A dry-run, expired, or conflicted plan cannot apply.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct TransferApply {
    #[serde(rename = "expected_revision")]
    pub expected_revision: String,
    #[serde(rename = "completed_paths", skip_serializing_if = "Option::is_none")]
    pub completed_paths: Option<Vec<String>>,
}

impl TransferApply {
    /// Push: verify staged bytes, recheck revision/writer, create and checkpoint replacement, atomically switch. Pull: completed_paths is an untrusted client receipt of local work, never proof of remote mutation; the remote snapshot stays unchanged. Partial receipts remain resumable. A dry-run, expired, or conflicted plan cannot apply.
    pub fn new(expected_revision: String) -> TransferApply {
        TransferApply {
            expected_revision,
            completed_paths: None,
        }
    }
}

