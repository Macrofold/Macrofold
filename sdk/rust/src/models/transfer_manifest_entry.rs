/*
 * Macrofold API
 *
 * Manage persistent projects, run cloud agents, stream progress, and integrate tools, billing, and operator reporting. Provider-owned OAuth, internal runtime ingress, and MCP JSON-RPC use separate contracts.
 *
 * The version of the OpenAPI document: 0.9.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// TransferManifestEntry : Explicit absent local file has null hash and zero bytes. baseline_known=false is distinct from a known absent baseline. Regular-file transfers only; reject symlink/device/directory conflicts. Runtime validates unique normalized paths and consistent hashes/sizes.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct TransferManifestEntry {
    /// Normalized relative POSIX path; runtime rejects traversal, NUL, absolute paths, unsafe links and reserved internal paths.
    #[serde(rename = "path")]
    pub path: String,
    /// SHA-256 of a regular file; null means absent.
    #[serde(rename = "local_sha256", deserialize_with = "Option::deserialize")]
    pub local_sha256: Option<String>,
    #[serde(rename = "local_size_bytes")]
    pub local_size_bytes: i32,
    #[serde(rename = "baseline_known")]
    pub baseline_known: bool,
    /// SHA-256 of a regular file; null means absent.
    #[serde(rename = "baseline_sha256", deserialize_with = "Option::deserialize")]
    pub baseline_sha256: Option<String>,
}

impl TransferManifestEntry {
    /// Explicit absent local file has null hash and zero bytes. baseline_known=false is distinct from a known absent baseline. Regular-file transfers only; reject symlink/device/directory conflicts. Runtime validates unique normalized paths and consistent hashes/sizes.
    pub fn new(path: String, local_sha256: Option<String>, local_size_bytes: i32, baseline_known: bool, baseline_sha256: Option<String>) -> TransferManifestEntry {
        TransferManifestEntry {
            path,
            local_sha256,
            local_size_bytes,
            baseline_known,
            baseline_sha256,
        }
    }
}

